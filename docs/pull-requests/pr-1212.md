---
type: pull_request
state: open
branch: feature/1205-workflow-model → dev
created: 2026-07-18T16:02:15Z
updated: 2026-07-18T16:02:15Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devkit/pull/1212
comments: 0
labels: none
assignees: none
milestone: none
projects: none
synced: 2026-07-19T05:29:39.471Z
---

# [PR 1212](https://github.com/vig-os/devkit/pull/1212) feat(workspace): add DEVKIT_WORKFLOW knob (gitflow default / trunk opt-in)

Implements epic #1205 — a per-consumer `DEVKIT_WORKFLOW` knob selecting the branching/release model. **gitflow stays the unchanged default; trunk is opt-in.**

## What it does
- `DEVKIT_WORKFLOW=gitflow|trunk`, **empty ⇒ gitflow** (existing consumers' `.vig-os` stays byte-identical).
- **gitflow (default):** unchanged — `dev` integration branch, cut `release/X.Y.Z` from `dev`, RC train, finalize→`main`+tag, `sync-main-to-dev` back-merge.
- **trunk:** `feature|bugfix|chore` straight to `main`; releases STILL happen but cut `release/X.Y.Z` **from `main`**, RC on it, merge back **into `main`** + tag. Only the `dev` branch and `sync-main-to-dev.yml` disappear; the whole downstream tag/build/publish/promote chain is unchanged (already `release/* → main`).

## How (design)
Realized **entirely at scaffold time** (mirrors `DEVKIT_MODE`) — an anchored `dev→main` render of the scaffolded workflows/skill/pre-commit plus a `sync-main-to-dev.yml` copy-exclude; NO resolve-toolchain runtime wiring, NO workflow twin. Enabled by the verified fact that every `dev` reference in `prepare-release.yml` is a plain branch literal (incl. the #617 read-after-write guard). ADR: `docs/rfcs/ADR-workflow-model.md`.

## Sub-issues delivered (spike #1206 + #1207–#1211)
- **#1207** manifest key + resolution + enum/contradiction guards + conditional writeback (trunk only).
- **#1208** `render_workflow_model()` in init-workspace.sh + sync-main-to-dev exclude/prune + `--force` preview mirror.
- **#1209** `--workflow` flag on install.sh/init-workspace.sh + dev-branch creation gating.
- **#1210** trunk coverage across the dev-assuming suites + new bats; `test_workflow_model.py` (19) end-to-end.
- **#1211** docs (`RELEASE_CYCLE`/`DOWNSTREAM_RELEASE`/`MIGRATION` "Workflow models", README/CLAUDE), ADR, plan doc.

## Verification
- Full hook suite green.
- **1094 pytest passed** incl. the new `test_workflow_model.py` and the trunk-extended suites; the **gitflow scaffold is byte-identical to today** (default-path regression guard). bats: install 99 ok, init-workspace 229 ok (#991 template invariants intact).
- 2 failures remain (`vig-utils` `TestFindRepoRoot`) — **pre-existing + environmental**, in a package this PR does not touch.

## Notes
- The three code sub-issues (#1207–#1209) share `init-workspace.sh`/`install.sh`, so they're consolidated into this one PR (per-sub-issue commits kept for traceability) rather than conflict-prone parallel PRs.
- Switching an existing repo between models is refused by a contradiction guard (destructive; a gitflow→trunk switch leaves an orphan remote `dev` — documented cleanup in MIGRATION).
- Scope: devkit capability only. Targets 1.5.0; independent of the in-flight 1.4.0 promote.

Refs: #1205, #1206, #1207, #1208, #1209, #1210, #1211
