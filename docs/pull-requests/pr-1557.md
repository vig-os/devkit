---
type: pull_request
state: closed (merged)
branch: release/1.11.0 → main
created: 2026-08-20T11:18:34Z
updated: 2026-08-20T13:04:18Z
author: vig-os-release-app[bot]
author_url: https://github.com/vig-os-release-app[bot]
url: https://github.com/vig-os/devkit/pull/1557
comments: 1
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-08-20T13:04:15Z
synced: 2026-08-21T03:09:55.980Z
---

# [PR 1557](https://github.com/vig-os/devkit/pull/1557) chore: release 1.11.0

# [Release 1.11.0](https://github.com/vig-os/devkit/releases/tag/1.11.0) - 2026-08-20

This PR prepares release 1.11.0 for merge to main.

## [1.11.0](https://github.com/vig-os/devkit/releases/tag/1.11.0) - 2026-08-20

### Added

- **A security exception now gives seven days' notice before it fails
  everything** ([#1552](https://github.com/vig-os/devkit/issues/1552))
  - `check-expirations` runs in all PR CI, in pre-commit, in both nightly scan
    lanes and in the release train, so one lapsed block reds every open branch
    at once — three times in two months
    ([#1260](https://github.com/vig-os/devkit/issues/1260),
    [#1481](https://github.com/vig-os/devkit/issues/1481),
    [#1547](https://github.com/vig-os/devkit/issues/1547)) — with no prior
    signal, even though every date had been chosen deliberately weeks earlier
  - `check-expirations` gains `--warn-days N` (emit `::warning::` annotations
    for entries falling due within N days, exit code untouched) and `--json`
    (emit the `expired`/`expiring` classification on stdout). The flags compose,
    and they keep the utility the single parser of the `Expiration:` grammar —
    no caller re-implements it
  - The nightly `security-scan` gains a third, non-failing issue class: one
    deduplicated notice per scanned ref per distinct upcoming expiry date,
    titled `Security exception register (<ref>): exceptions expire <date>`,
    covering `.vulnixignore`, `.trivyignore` **and**
    `.github/dependency-review-allow.txt`
  - The step runs *before* the blocking validation step, so an already-red
    register still reports what is coming next, and it is `continue-on-error`
    so a missed notice can never turn a green scan red
  - Seven days is one expiry-grid period: dates land on a Wednesday, so the
    notice does too, one full Renovate cycle ahead of the red. The issue body
    spells out that a renewal is a re-verification, not a date bump — read the
    findings delta, delete what the pin advance cleared — because the notice
    deliberately arrives before that data exists
  - Default behaviour is unchanged: no existing call site, exit code or output
    moves

- **A failed `devkit-upgrade` now leaves a tracking issue in the consumer repo**
  ([#1530](https://github.com/vig-os/devkit/issues/1530))
  - The adoption branch only reaches the remote in the publish step, so a
    failure before it (version resolve, scaffold run, in-shell commit) was
    visible only as a red scheduled run — and the consumer's `DEVKIT_VERSION`
    then stopped moving, silently, week after week
  - A new `report` job files **one** issue per repo carrying the run URL, the
    failing step (read off the run's own job records) and the pinned vs target
    version; every repeat failure comments on that same issue, and the next
    fully green run closes it with a link to the green run
  - Not a revert of [#1405](https://github.com/vig-os/devkit/issues/1405): that
    dropped the per-adoption issue on the **success** path, where the bot PR is
    the artifact. This one exists only where there is no artifact
  - Reporting is best-effort by design: the `issues: write` grant is minted as
    a **separate**, `continue-on-error` token, so an App installation that
    never received it degrades to a warning instead of turning a working
    upgrade red. Grant `issues: write` to the devkit-upgrade App to switch
    reporting on

### Changed

- **Security-exception expiries are staggered across distinct Wednesdays**
  ([#1553](https://github.com/vig-os/devkit/issues/1553))
  - 29 of the 33 live exception entries expired on the same day, `2026-09-02`:
    24 in `.vulnixignore` (7 blocks), 4 in `.trivyignore` (3 blocks) and the
    one `.github/dependency-review-allow.txt` entry. One stalled review on that
    date would have taken every open PR's CI, both nightly `security-scan`
    lanes and the release train red at once — the cliff that
    `docs/CONTAINER_SECURITY.md` already warns about ("stagger across weeks,
    not weekdays")
  - Each block now owns a distinct Wednesday, spread by week-multiples and
    ordered by how soon its remediation lever can move: podman keeps
    `2026-09-02` as the anchor (weekly re-check, backport open), then unbound
    `09-09`, the libssh2 6603x batch `09-16`, libssh2 `CVE-2026-58050` `09-23`,
    the lower-reachability batch `09-30`, fzf `10-07` and glibc `10-14` (no
    rev-advance lever exists, so it takes the longest window). The
    `.trivyignore` blocks follow at `10-21`/`10-28`/`11-04` and the
    dependency-review allow-list at `11-11`, keeping that register's review off
    the vulnix weeks
  - The Class-1 CPE-mismatch block keeps its yearly `2027-06-23`
  - Scheduling only: no risk assessment was re-opened and no entry was added or
    removed. Every re-dated block records the move and its justification in the
    register's own note style

#### Dependencies

- Update `github/codeql-action` from `5595cca` to `ff2f1c6` ([#1524](https://github.com/vig-os/devkit/pull/1524))
- Update `aquasecurity/trivy` from `v0.73.0` to `v0.74.0` ([#1525](https://github.com/vig-os/devkit/pull/1525))
- Update `cachix/install-nix-action` from `v31.11.0` to `v31.11.1` ([#1525](https://github.com/vig-os/devkit/pull/1525))
- Update `astral-sh/setup-uv` from `v9.0.0` to `v10.0.1` ([#1526](https://github.com/vig-os/devkit/pull/1526))
- Lock file maintenance (pip) ([#1527](https://github.com/vig-os/devkit/pull/1527), [#1556](https://github.com/vig-os/devkit/pull/1556))

### Fixed

- **`just doctor` no longer calls a tilde signing-key path incomplete**
  ([#1546](https://github.com/vig-os/devkit/issues/1546))
  - `user.signingkey = ~/.ssh/<key>.pub` is a working SSH-signing setup — git
    expands the leading `~/` when it consumes the value — but the readability
    guard tested it with `test -r`, which performs no tilde expansion, so a
    correctly configured host was told its signing was `incomplete`
  - The guard now expands a leading `~/` against `$HOME` before the test, in
    both the devkit `justfile` and the scaffolded `assets/workspace/justfile`.
    The `PASS` line still reports the raw value, so it keeps mirroring
    `git config`

- **A nightly security scan that fails before the gate now files a tracking
  issue too** ([#1548](https://github.com/vig-os/devkit/issues/1548))
  - The issue-opening step was guarded on `steps.vulnix-gate.outcome ==
    'failure'` — an outcome that is *empty* whenever an earlier step fails. An
    expired `.vulnixignore` register, a failed closure build or a vulnix crash
    therefore produced a red run and nothing else, on a workflow whose whole
    point is that a scheduled run has no other signal
  - It fired for real on 2026-08-20: an expired block failed the job's first
    step on both lanes and surfaced nowhere
    ([#1547](https://github.com/vig-os/devkit/issues/1547))
  - The guard is now `failure()` alone, and the two failure classes carry
    distinct titles that dedup independently per ref: a red gate keeps its
    existing title, while a pre-gate failure files *run failed before the vulnix
    gate* and names the failing step, read off the run's own job records
    (`actions: read`). A pre-gate failure means the closure went **unscanned**,
    not clean — the body says so
- **Release CI gate now counts only the latest run of each check**
  ([#1522](https://github.com/vig-os/devkit/issues/1522))
  - The gate counted **every** FAILURE entry in the release PR head SHA's
    `statusCheckRollup`. A superseded workflow run leaves its check runs
    attached to that same SHA, so the gate refused a branch `gh pr checks` —
    which keeps only the latest run per name — reported green. During the
    1.10.0 train that forced deleting the superseded run to proceed
    ([#1516](https://github.com/vig-os/devkit/issues/1516))
  - All six copies of the gate now group the rollup by check name and evaluate
    the most recent run of each: devkit's `release.yml` and `promote-release.yml`
    (validate and merge), and the consumer `release-core.yml` and
    `promote-release.yml` (validate and merge). A re-run still in flight is the
    latest, so it holds the gate open as before, and a genuinely failing latest
    run still blocks
- **Release CI gate now reads commit-status results**
  ([#1538](https://github.com/vig-os/devkit/issues/1538))
  - The gate classified every `statusCheckRollup` entry by `.conclusion`, a
    field only Checks-API entries carry. A commit status reports `state`
    instead, so it always landed in the pending bucket and held the gate open
    **forever**: a red status could never block, a green one never stop
    counting as pending
  - All six copies now classify each entry on a normalized verdict that falls
    back from `.conclusion` to `.state`: `FAILURE`/`ERROR` block,
    `PENDING`/`EXPECTED` wait, `SUCCESS` counts as a passing check. Checks-API
    entries are unaffected — an in-progress one still resolves to null and
    counts as pending
  - Latent today (nothing in the org publishes commit statuses); it becomes
    live the day a consumer adopts a tool that reports through the status API
- **`just gh-issues` CI column now shows the live re-run, not the failure it
  supersedes**
  ([#1539](https://github.com/vig-os/devkit/issues/1539))
  - The PR table's latest-per-name check dedup keyed recency on `completedAt`,
    which is null while a re-run is in flight: the live run ranked as the
    *oldest* run of its name, so the column reported the superseded FAILURE
    during exactly the window someone watches a re-run
  - Recency is now keyed on `startedAt` (set at creation, never null) with a
    `createdAt` fallback for commit statuses, matching the release CI gates
    ([#1522](https://github.com/vig-os/devkit/issues/1522)). Display-only — the
    table is not a gate
- **`just gh-issues` CI column now reads commit-status results**
  ([#1544](https://github.com/vig-os/devkit/issues/1544))
  - The PR table grouped the rollup by `name` and classified each entry by
    `conclusion`, both fields only Checks-API entries carry. Every commit
    status on a PR collapsed into a single `"?"` bucket, and whatever survived
    rendered as pending: a red status never showed as failed, a green one never
    counted toward the pass tally
  - Grouping now keys on name-or-context and each entry is classified on a
    normalized verdict that falls back from `conclusion` to `state`, so the
    table reads a rollup exactly as the release CI gates do
    ([#1522](https://github.com/vig-os/devkit/issues/1522),
    [#1538](https://github.com/vig-os/devkit/issues/1538)). Checks-API entries
    render as before. Display-only — the table is not a gate
- **Describing a bot-identity bug no longer trips the agent-fingerprint check**
  ([#1521](https://github.com/vig-os/devkit/issues/1521))
  - The `emails` blocklist was matched as a bare substring over the whole
    content, while `names` had carried an attribution-context guard since
    [#274](https://github.com/vig-os/devkit/issues/274). A changelog entry that
    quoted a CI bot identity in an inline code span therefore blocked its own
    release PR, because `prepare-release` renders the changelog section into the
    PR body — exactly what stalled the 1.10.0 train
    ([#1516](https://github.com/vig-os/devkit/issues/1516))
  - `contains_agent_fingerprint` now strips inline code spans before matching
    `emails`, so a quoted identity reads as prose. Matching stays
    case-insensitive, `names` and trailer rules are untouched, and a plain-text
    attribution — a `Co-authored-by` trailer, a sign-off, an author line —
    still fails
- **Generated mirror-fold comment now lints under the stock consumer typos
  seed** ([#1529](https://github.com/vig-os/devkit/issues/1529))
  - A comment the `DEVKIT_SYNC_TARGET` block renders into the managed
    `release-core.yml` used a token that only devkit's own `.typos.toml`
    allowlists, so every mirror-mode consumer whose seeded `.typos.toml`
    predates that entry failed its `devkit-upgrade` at the commit step —
    `.typos.toml` is seeded once and never overwritten, so the consumer could
    not receive the entry. Reworded; the tree the scaffold generates is now
    typo-clean with no allowlist at all
  - Pinned by a new bats fixture that renders a mirror-mode workspace in both
    workflow models and runs `typos` (no allowlist) plus `actionlint` over it
    ([#1531](https://github.com/vig-os/devkit/issues/1531)) — the fold block
    has one consumer in the org, so nothing else linted it
- **Synced devkit changelog no longer leans on a typos allowlist entry the
  consumer may not have**
  ([#1534](https://github.com/vig-os/devkit/issues/1534))
  - `assets/workspace/.devcontainer/CHANGELOG.md` is a manifest-synced copy of
    the root changelog, and devcontainer-mode consumers **git-track** it — so
    their own typos hook lints it against a `.typos.toml` that is seeded once
    and never overwritten. The released 1.10.0 entry carries two hyphenated
    compounds that lint only under
    [#1488](https://github.com/vig-os/devkit/issues/1488)'s allowlist entry, so
    a consumer seeded before that release would fail its upgrade at the commit
    step, exactly as org-config did in
    [#1529](https://github.com/vig-os/devkit/issues/1529) — and neither of the
    two config files that could fix it is ever overwritten
  - Released changelog text is immutable, so the manifest de-hyphenates those
    two spellings in the generated **dest** only: the root file keeps its
    released bytes and the mirror is clean under `typos --isolated`, with no
    allowlist at all
  - A new devkit-only `check-unreleased-typos` hook lints the `## Unreleased`
    section with no allowlist before it can be committed, so no future release
    can freeze another seed-dependent word into immutable text — catching one
    after the release is useless. Released sections keep their allowlisted
    tokens and are out of scope
  - The [#1531](https://github.com/vig-os/devkit/issues/1531) render lint gains
    a devcontainer-mode leg: that tracked tree — synced changelog and
    `version-check.sh` included — must need no allowlist entry newer than the
    grandfathered words every live consumer's seed predates

### Security

- **`devkit-upgrade` fetches `install.sh` from the target release tag, not
  `main`** ([#1532](https://github.com/vig-os/devkit/issues/1532))
  - The managed workflow ran a `main`-tip installer while scaffolding a pinned
    `$TARGET` payload — an untested pairing, and the one every *scheduled*
    consumer run got whenever `main` was ahead of the latest release. It also
    meant any push to devkit `main` immediately changed code executing with
    `contents: write` in every consumer repo, with no review gate
  - The fetch is now `refs/tags/${TARGET}/install.sh`, so installer and payload
    move together and only a published release can change what runs. No `main`
    fallback is needed: the resolve step admits only `X.Y.Z[-prerelease]`
    versions, and every devkit tag of that shape carries a root-level
    `install.sh`
  - Scorecard's `Pinned-Dependencies` finding on the consumer copy is expected
    to remain — it wants a commit SHA, and SHA-pinning a self-upgrading
    installer is circular by construction; dismiss it consumer-side
- **Renew the fzf exception block in the vulnix register**
  ([#1547](https://github.com/vig-os/devkit/issues/1547))
  - The block expired 2026-08-19, failing `check-expirations` on every branch
    (the hook runs inside the flake's `pre-commit` check) and on both lanes of
    the nightly security scan, which aborts before vulnix ever runs
  - Re-verified against the current pin before renewing: both CVEs are still
    live findings against `fzf-0.72.0`, and `nixos-26.05`, `release-26.05` and
    `staging-26.05` all still ship 0.72.0 with no backport PR open, so
    advancing the pin cannot clear them either
  - Renewed onto the shared `2026-09-02` grid date, so the whole register comes
    up for review in one pass. The risk assessment is unchanged: both findings
    are availability-only and unreachable on this image (32-bit-only overflow;
    opt-in `--listen` server)



---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/devkit/pull/1557#issuecomment-5355795256) by [@c-vigo](https://github.com/c-vigo)

_Posted on August 20, 2026 at 12:20 PM_

### rc1 validation sweep — 13 checks, all pass

Run against the **shipped `ghcr.io/vig-os/devcontainer:1.11.0-rc1` image**, not the source tree, so every result reflects what actually ships. Consumer checks ran on throwaway copies; no real repo was modified.

#### Artifacts

| Check | Result |
|---|---|
| SLSA provenance (`gh attestation verify`) | ✅ built by `release.yml@refs/heads/release/1.11.0` |
| Multi-arch index | ✅ `linux/amd64` + `linux/arm64` |
| Compressed size vs 1.10.0 | 0.574 → 0.581 GiB (amd64), 0.567 → 0.575 (arm64) — **+1.3%**, matches the dependency bumps, no bloat |

#### #1552 — advance expiry notice

The risk here was a **stale nix-store `vig-utils`** silently ignoring the new flags. It does not: `check-expirations --help` in the image shows both `--warn-days N` and `--json`.

Boundary math against this branch's real register:

| `--warn-days` | Warnings | Exit |
|---|---|---|
| 7 | 0 | 0 |
| 12 | 0 | 0 |
| **13** | **fires on the podman anchor** — `CVE-2026-57231`, expires `2026-09-02`, 13 days left | 0 |
| 14 | same | 0 |

Exit code never moves, as designed. Flags compose: JSON on stdout, `::warning::` on stderr, so a JSON consumer is not polluted.

> This arithmetically confirms the promote-by date: **`2026-09-02` − 7 = `2026-08-26`**.

#### #1553 — staggered register

12 blocks on 12 distinct Wednesdays (`09-02` … `11-11`, plus the untouched `2027-06-23` Class-1 block), each date appearing exactly once. Baseline validates **33 exceptions across 3 files**, exit 0. The vulnix gate in the rc1 run passed against this register for real.

#### #1546 — `just doctor` tilde signing key

Five cases against the shipped `justfile`:

| Case | Expected | Got |
|---|---|---|
| `~/key.pub`, file exists | PASS | ✅ PASS, and the line prints the **raw** `~/testkey.pub`, not the expansion |
| `~/key.pub`, file **missing** | WARN | ✅ WARN — the twin holds, expansion did not become a rubber stamp |
| absolute path, exists | PASS | ✅ |
| literal `ssh-ed25519 …` | PASS | ✅ |
| `commit.gpgsign=false` | WARN | ✅ |

#### #1529 / #1534 — consumer upgrade rehearsal

The highest-value check, since CI only proves the *render*, not the *upgrade*.

**#1529:** copied `org-config` (1.9.0, direnv, trunk), **deleted its `mis = "mis"` workaround** to restore a pristine pre-#1488 seed, then ran `install.sh --version 1.11.0-rc1 --force`. Upgrade succeeded, pin → `1.11.0-rc1`, and **`typos` exits 0** — `mis-parse` / `mis-splitting` are absent from all generated content.

> **Follow-up:** `org-config` can drop `mis = "mis"` from its `.typos.toml` once it takes 1.11.0, exactly as that entry's own comment instructs.

**#1534:** on a devcontainer-mode consumer, `.devcontainer/CHANGELOG.md` carries **0** hyphenated and **2** de-hyphenated tokens (`misparses` / `missplitting`, lines 389/391), and lints **exit 0** under a frozen pre-#1488 seed.

#### #1532 / #1530 — rendered consumer workflow

- `devkit-upgrade.yml` fetches `…/vig-os/devkit/refs/tags/${TARGET}/install.sh` — the target tag, not `main`.
- The `report:` job is present and carries the load-bearing `!cancelled()` guard with its rationale comment.

#### #1522 / #1538 / #1539 / #1544 — CI-gate fixes

**#1522 and #1538 were exercised live** in this train's `Validate Release` job — first real run of both — and passed.

**#1539 proven decisively.** Feeding the shipped `vig_utils.gh_issues` an old `FAILURE` (with `completedAt`) alongside a live `IN_PROGRESS` re-run (`completedAt: null`), `_dedupe_status_checks` picks the **live** one. That is precisely the reported bug.

**#1538 / #1544 cannot be proven from live data — worth recording.** There is **no `StatusContext` anywhere in this org**: 5 devkit PRs and 3 smoke-test PRs are 100% `CheckRun`, including the `CodeQL` entry, which is a `CheckRun` despite its empty `workflowName`. These fixes are therefore **defensive and latent**, covered only by unit tests. Verified indirectly by feeding the shipped helpers synthetic shapes — `_check_key` / `_verdict` resolve `context`/`state` for `StatusContext` and `name`/`conclusion` for `CheckRun`, and a pending `StatusContext` classifies as pending rather than being mis-bucketed.

#### Two observations, neither blocking

1. **`flake-bump` advanced `org-config`'s `vigos` input to `c624765`** (main tip, 1.10.0-era) rather than the rc1 tag — expected, since rc1 is not on `main`. Worth confirming it lands on 1.11.0 after promote, per the 1.6.0 lesson that final-lane bumps are never pin-only for direnv consumers.
2. A bare `typos` run surfaced ~10 errors under `docs/pull-requests/*.md` in the smoke-test copy. These are **pre-existing and out of scope**: that repo excludes `^.github_data/|^docs/issues/|^docs/pull-requests/` at the top of its pre-commit config (347 tracked legacy files), and a bare invocation simply bypasses that exclude. Not a scaffold regression.

#### Still inert until `dev` → `main`

Scheduled runs execute the workflow file from the **default branch**, so #1552's advance notice, #1548's pre-gate failure reporting and the #1547/#1553 register dates are all inert on `main` today. `main` still carries the expired fzf block, so its nightly lane fails every night and — lacking #1548 — files no issue. All three clear on promote.


---
---

## Commits

### Commit 1: [fadc57b](https://github.com/vig-os/devkit/commit/fadc57bb423d7e3538cd83a82c1898e7148e5c4f) by [vig-os-release-app[bot]](https://github.com/apps/vig-os-release-app) on August 14, 2026 at 05:07 PM
chore: sync dev with main (#1520), 2094 files modified

### Commit 2: [6cc0580](https://github.com/vig-os/devkit/commit/6cc05806082f3448f5003d085d1aa4ca5e07717b) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 15, 2026 at 02:58 AM
chore: sync issues and PRs, 479 files modified (docs/issues/issue-1519.md, docs/issues/issue-1521.md, docs/issues/issue-1522.md, docs/issues/issue-1523.md, docs/pull-requests/pr-1515.md, docs/pull-requests/pr-1520.md)

### Commit 3: [65aa8e7](https://github.com/vig-os/devkit/commit/65aa8e77305458f2290b6e8c66cf6db7ef17fc1d) by [renovate[bot]](https://github.com/apps/renovate) on August 17, 2026 at 01:32 AM
chore(deps): update github/codeql-action digest to ff2f1c6, 12 files modified (.github/workflows/codeql.yml, .github/workflows/scorecard.yml, assets/workspace/.github/workflows/codeql.yml, assets/workspace/.github/workflows/scorecard.yml)

### Commit 4: [d4885e4](https://github.com/vig-os/devkit/commit/d4885e4f8c261bc812584ccdbe2d1c854ccc2853) by [renovate[bot]](https://github.com/apps/renovate) on August 17, 2026 at 01:32 AM
ci(actions): update github-actions (minor and patch), 28 files modified

### Commit 5: [9180ee4](https://github.com/vig-os/devkit/commit/9180ee41cc7f4dd56f8372dc9249a3585a024af9) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 17, 2026 at 03:06 AM
chore: sync issues and PRs, 164 files modified (docs/issues/issue-529.md, docs/pull-requests/pr-1524.md, docs/pull-requests/pr-1525.md)

### Commit 6: [9b3fc2b](https://github.com/vig-os/devkit/commit/9b3fc2b0137e578389c51fe3bccdcae4be66dff7) by [renovate[bot]](https://github.com/apps/renovate) on August 17, 2026 at 04:51 AM
ci(actions): update astral-sh/setup-uv action to v10, 2 files modified (assets/workspace/.github/actions/setup-devkit-toolchain/action.yml)

### Commit 7: [5bc1986](https://github.com/vig-os/devkit/commit/5bc1986f5d93e6c686745bc8080941d5cd06d161) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 17, 2026 at 05:31 AM
chore(nix): bump nixpkgs-unstable (fast-movers refresh), 6 files modified (flake.lock)

### Commit 8: [8efc565](https://github.com/vig-os/devkit/commit/8efc56527312aa285f4946d5e6f2fa282b21d70b) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 07:09 AM
chore(deps): update github/codeql-action digest to ff2f1c6 (#1524), 12 files modified (.github/workflows/codeql.yml, .github/workflows/scorecard.yml, assets/workspace/.github/workflows/codeql.yml, assets/workspace/.github/workflows/scorecard.yml)

### Commit 9: [e02ffc4](https://github.com/vig-os/devkit/commit/e02ffc4f73efaedea7ef283780495ceecce67c84) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 07:09 AM
ci(actions): update github-actions (minor and patch) (#1525), 28 files modified

### Commit 10: [690535b](https://github.com/vig-os/devkit/commit/690535b912df2379e65d9a07e94d4e4b55ca4a50) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 08:31 AM
test(scaffold): lint the mirror-fold render as a stock consumer would, 110 files modified (tests/bats/release-mirror-fold-lint.bats)

### Commit 11: [bc01429](https://github.com/vig-os/devkit/commit/bc014294980897d3e9a69a1f50d34fa8d5f5de6d) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 08:33 AM
fix(workspace): reword mirror-fold comment to lint under a stock typos seed, 30 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 12: [47aa53a](https://github.com/vig-os/devkit/commit/47aa53a82b97f0c5ae43711407517747b4e7c2dc) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:02 AM
fix(workspace): reword mirror-fold comment to lint under a stock typos seed (#1533), 140 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, tests/bats/release-mirror-fold-lint.bats)

### Commit 13: [0125386](https://github.com/vig-os/devkit/commit/012538684d40190437406710c902d78dd10fb353) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:13 AM
test(upgrade): pin the devkit-upgrade failure report, 372 files modified (tests/bats/devkit-upgrade-alert.bats, tests/test_workflow_devkit_upgrade.py)

### Commit 14: [0686afa](https://github.com/vig-os/devkit/commit/0686afa999f4dacbd1bd66c6f65239922db82c50) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:16 AM
feat(upgrade): report a failed devkit upgrade as a tracking issue, 175 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/devkit-upgrade.yml)

### Commit 15: [895f951](https://github.com/vig-os/devkit/commit/895f951640b0aa87a0e9227e05b888c67d3ba1f2) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:36 AM
fix(upgrade): gate the report job with !cancelled() so it fires on failure, 9 files modified (assets/workspace/.github/workflows/devkit-upgrade.yml, tests/test_workflow_devkit_upgrade.py)

### Commit 16: [bddad03](https://github.com/vig-os/devkit/commit/bddad03dd7d546a76f165aeecd7599c4cca37eba) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:50 AM
ci(actions): update astral-sh/setup-uv action to v10 (#1526), 2 files modified (assets/workspace/.github/actions/setup-devkit-toolchain/action.yml)

### Commit 17: [1f6fcae](https://github.com/vig-os/devkit/commit/1f6fcae4edc001611bb07a17b68ec8b6ea1ad68f) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:50 AM
chore(nix): bump nixpkgs-unstable (fast-movers refresh) (#1528), 6 files modified (flake.lock)

### Commit 18: [9b86915](https://github.com/vig-os/devkit/commit/9b86915cd2a56b9b420c40cb92bef0676f598423) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:51 AM
feat(upgrade): report a failed devkit upgrade as a tracking issue (#1535), 554 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/devkit-upgrade.yml, tests/bats/devkit-upgrade-alert.bats, tests/test_workflow_devkit_upgrade.py)

### Commit 19: [cbe6096](https://github.com/vig-os/devkit/commit/cbe6096f20dc4757ab27c33cd28f6db538624df5) by [renovate[bot]](https://github.com/apps/renovate) on August 17, 2026 at 09:52 AM
build(pip): lock file maintenance, 101 files modified (uv.lock)

### Commit 20: [af928ef](https://github.com/vig-os/devkit/commit/af928ef9636d623ed37ab922005500b80f327ec3) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:55 AM
test(vig-utils): pin prose identity mentions against the fingerprint gate, 177 files modified (packages/vig-utils/tests/test_check_pr_agent_fingerprints.py, packages/vig-utils/tests/test_utils.py)

### Commit 21: [b74e0f9](https://github.com/vig-os/devkit/commit/b74e0f9cf2e0f26277069847c543b26f160d6254) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:55 AM
fix(vig-utils): skip inline code spans when matching blocked emails, 13 files modified (packages/vig-utils/src/vig_utils/utils.py)

### Commit 22: [e1265ed](https://github.com/vig-os/devkit/commit/e1265ed33179cafbd823889258e8f41cfe0f2b4f) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 09:56 AM
docs(changelog): record the fingerprint-check code-span fix, 28 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 23: [c153641](https://github.com/vig-os/devkit/commit/c153641e0b54bfb87755100d01f856a88951d703) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 10:05 AM
build(pip): lock file maintenance (#1527), 101 files modified (uv.lock)

### Commit 24: [4f2d2a9](https://github.com/vig-os/devkit/commit/4f2d2a9b583f9980f18df161cdbedbbc719049b5) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 10:19 AM
fix(vig-utils): skip inline code spans when matching blocked emails (#1536), 218 files modified

### Commit 25: [5cc6bb4](https://github.com/vig-os/devkit/commit/5cc6bb4c060bf385eb3b406adb23bf5c9e651005) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 10:25 AM
test(release): execute the CI-green gate against fixture rollups, 304 files modified (tests/test_ci_green_gate.py, tests/workflow_scaffold.py)

### Commit 26: [bb4bd62](https://github.com/vig-os/devkit/commit/bb4bd626e8e2f0af3fee45b499495e49b5a112d1) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 10:42 AM
fix(release): count only the latest check run per name in the CI-green gate, 120 files modified

### Commit 27: [24a097a](https://github.com/vig-os/devkit/commit/24a097ab841d8cc823dfda73c7d8b59ede0aecd2) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 10:57 AM
fix(release): count only the latest check run per name in the CI-green gate (#1537), 424 files modified

### Commit 28: [2a1626e](https://github.com/vig-os/devkit/commit/2a1626e607897cc325a12542dfd891b1c23b45c5) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 11:51 AM
test(upgrade): pin the installer fetch to the target release tag, 36 files modified (tests/test_workflow_devkit_upgrade.py)

### Commit 29: [f20057c](https://github.com/vig-os/devkit/commit/f20057cb72fc3fe3826213a7fdef8f0d866b716d) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 11:52 AM
fix(upgrade): fetch install.sh from the target release tag, 43 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/devkit-upgrade.yml)

### Commit 30: [e853dcb](https://github.com/vig-os/devkit/commit/e853dcbd2a0eb6aa6e7497e905b694e16e96fef3) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 12:09 PM
fix(upgrade): fetch install.sh from the target release tag (#1540), 79 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/devkit-upgrade.yml, tests/test_workflow_devkit_upgrade.py)

### Commit 31: [ced391d](https://github.com/vig-os/devkit/commit/ced391da40170b44b26474a525c99861dc38da2c) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 12:12 PM
test(release): pin commit-status classification in the CI-green gate, 123 files modified (tests/test_ci_green_gate.py)

### Commit 32: [17a130e](https://github.com/vig-os/devkit/commit/17a130e27e93450c67660fe6ecc381fae9926d65) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 12:14 PM
fix(release): classify commit-status states in the CI-green gate, 93 files modified

### Commit 33: [4e950b6](https://github.com/vig-os/devkit/commit/4e950b616aafab91d0a79632824f967ee04ac441) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 12:30 PM
fix(release): classify commit-status states in the CI-green gate (#1541), 216 files modified

### Commit 34: [256f0be](https://github.com/vig-os/devkit/commit/256f0beb9904c77b087c3a631895cdc8ac3edfa7) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 12:32 PM
test(vig-utils): pin check dedup recency on startedAt, 126 files modified (packages/vig-utils/tests/test_gh_issues.py)

### Commit 35: [9f2a505](https://github.com/vig-os/devkit/commit/9f2a5050ceba34adeb4f09ceab28a4a324620791) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 12:32 PM
fix(vig-utils): key check dedup recency on startedAt, 20 files modified (packages/vig-utils/src/vig_utils/gh_issues.py)

### Commit 36: [5434086](https://github.com/vig-os/devkit/commit/543408689f19ddeaf1a250883078f9c03b64bf63) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 12:33 PM
docs(changelog): note the gh-issues check dedup recency fix, 22 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 37: [d0cbf22](https://github.com/vig-os/devkit/commit/d0cbf22f66adb157e0018a06f9be103182a827c4) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 12:49 PM
fix(vig-utils): key check dedup recency on startedAt (#1542), 168 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, packages/vig-utils/src/vig_utils/gh_issues.py, packages/vig-utils/tests/test_gh_issues.py)

### Commit 38: [04e4b25](https://github.com/vig-os/devkit/commit/04e4b25e3c28406434abab1bc07c76ed47fd6dab) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 01:01 PM
test(changelog): pin the synced mirror against seed-dependent typos, 282 files modified (tests/bats/release-mirror-fold-lint.bats, tests/test_changelog_unreleased_typos.py, tests/test_flake_hooks.py, tests/test_transforms.py)

### Commit 39: [a3989a5](https://github.com/vig-os/devkit/commit/a3989a5c3c255b98b73f8de1f3af1e1f25a2c273) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 01:05 PM
fix(workspace): sanitize the synced changelog and guard unreleased entries, 212 files modified

### Commit 40: [c58de1f](https://github.com/vig-os/devkit/commit/c58de1f0c4457f20fa8b7c4f753c25cf27713beb) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 01:06 PM
docs(changelog): note the synced-changelog typo sanitization, 52 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 41: [dfd2e77](https://github.com/vig-os/devkit/commit/dfd2e775af1f6120ad27b4a2a7591c3461a7a16d) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 01:25 PM
fix(workspace): sanitize the synced changelog and guard unreleased entries (#1543), 546 files modified

### Commit 42: [0b6f556](https://github.com/vig-os/devkit/commit/0b6f556025a22b9daa5c0636f9fb482721c5bb9e) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 02:52 PM
test(vig-utils): pin StatusContext dedup and rendering in the PR table, 141 files modified (packages/vig-utils/tests/test_gh_issues.py)

### Commit 43: [41faf0f](https://github.com/vig-os/devkit/commit/41faf0f9e3344a3d65bfbbe27a7df0389c1ef086) by [c-vigo](https://github.com/c-vigo) on August 17, 2026 at 02:53 PM
fix(vig-utils): render commit statuses in the gh-issues PR table, 67 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, packages/vig-utils/src/vig_utils/gh_issues.py)

### Commit 44: [0e6f9d0](https://github.com/vig-os/devkit/commit/0e6f9d0f4b240f3862f1c81ec03854c14c45c121) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 18, 2026 at 03:02 AM
chore: sync issues and PRs, 1506 files modified

### Commit 45: [b7154eb](https://github.com/vig-os/devkit/commit/b7154eb67053360dea0781754f1e867c078ebd23) by [c-vigo](https://github.com/c-vigo) on August 18, 2026 at 06:25 AM
fix(vig-utils): render commit statuses in the gh-issues PR table (#1545), 208 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, packages/vig-utils/src/vig_utils/gh_issues.py, packages/vig-utils/tests/test_gh_issues.py)

### Commit 46: [9782f3d](https://github.com/vig-os/devkit/commit/9782f3d37eaf6d84beb8bc36bb5516de1c636a82) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 19, 2026 at 03:04 AM
chore: sync issues and PRs, 154 files modified (docs/issues/issue-1544.md, docs/issues/issue-1546.md, docs/issues/issue-529.md, docs/pull-requests/pr-1545.md)

### Commit 47: [1648769](https://github.com/vig-os/devkit/commit/164876987d85e7c033e18a846abddf1dd450f61f) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 06:51 AM
fix(security): renew the fzf exception block to 2026-09-02, 37 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 48: [227dedc](https://github.com/vig-os/devkit/commit/227dedc0b1fbcccf2dcba1bfa2f1f5aa18021340) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 07:05 AM
fix(security): renew the fzf exception block to 2026-09-02 (#1549), 37 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 49: [422e3fe](https://github.com/vig-os/devkit/commit/422e3fe3eb449f44114f1ed0d27cfa1f82e28b34) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 07:06 AM
test(security-scan): pin that any scan failure files a tracking issue, 107 files modified (tests/test_workflow_scan_failure_reporting.py)

### Commit 50: [737ac87](https://github.com/vig-os/devkit/commit/737ac87ef6b059b84346deef17a85137a3e3fbf2) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 07:08 AM
fix(security-scan): file a tracking issue for pre-gate failures too, 122 files modified (.github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/CONTAINER_SECURITY.md)

### Commit 51: [fe593bb](https://github.com/vig-os/devkit/commit/fe593bbd1306912449c210acaf58a01d4745c886) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 07:22 AM
fix(security-scan): file a tracking issue for pre-gate failures too (#1550), 229 files modified (.github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/CONTAINER_SECURITY.md, tests/test_workflow_scan_failure_reporting.py)

### Commit 52: [8589e5a](https://github.com/vig-os/devkit/commit/8589e5ada9c2fc26ac415a78309baf6f3699de55) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 07:30 AM
test(doctor): pin that a tilde signing key path reports PASS, 53 files modified (tests/bats/consumer-doctor.bats, tests/bats/doctor.bats)

### Commit 53: [7376ed7](https://github.com/vig-os/devkit/commit/7376ed711756a7ef83ce0c8c4990495827640d9c) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 07:31 AM
fix(doctor): expand a leading tilde in user.signingkey before the readability test, 22 files modified (assets/workspace/justfile, justfile)

### Commit 54: [2019637](https://github.com/vig-os/devkit/commit/201963798b0c527903ebf8a7432612f8da5e0262) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 07:31 AM
docs(changelog): record the doctor tilde signing-key fix, 22 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 55: [9d37073](https://github.com/vig-os/devkit/commit/9d37073cab381e8a324376ad85ae8046b3ecd75c) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 07:52 AM
fix(doctor): expand a leading tilde in user.signingkey before the readability test (#1551), 97 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/justfile, justfile, tests/bats/consumer-doctor.bats, tests/bats/doctor.bats)

### Commit 56: [050fe04](https://github.com/vig-os/devkit/commit/050fe0435ce2b5876934128c868bd3a730f18ac9) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 08:40 AM
test(vigutils): pin --warn-days and --json on check-expirations, 219 files modified (packages/vig-utils/tests/test_check_expirations.py)

### Commit 57: [3170a59](https://github.com/vig-os/devkit/commit/3170a59ade291ca9c271c672d0d62eb47e0e9e0b) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 08:42 AM
feat(vigutils): add --warn-days and --json to check-expirations, 120 files modified (packages/vig-utils/src/vig_utils/check_expirations.py)

### Commit 58: [6da764e](https://github.com/vig-os/devkit/commit/6da764e73bdbd7dbdbc04d677bfefb2bc7ce6734) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 08:44 AM
chore(security): stagger exception expiries onto distinct Wednesdays, 137 files modified (.github/dependency-review-allow.txt, .trivyignore, .vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 59: [4ae883a](https://github.com/vig-os/devkit/commit/4ae883a600c091e5d5cd5ad7c7132a010af95952) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 08:54 AM
test(workflows): pin the seven-day exception expiry notice on security-scan, 190 files modified (tests/test_workflow_expiry_notice.py, tests/test_workflow_scan_failure_reporting.py)

### Commit 60: [bb35a93](https://github.com/vig-os/devkit/commit/bb35a932b3ca56ab6e26ecb987dbc36877041085) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 08:55 AM
ci(security-scan): open an advance notice 7 days before an exception expires, 100 files modified (.github/workflows/security-scan.yml)

### Commit 61: [727c19a](https://github.com/vig-os/devkit/commit/727c19ab9ec915f7fbe54fee9645425b8efe804f) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 08:55 AM
docs(security): document the seven-day exception expiry notice, 29 files modified (docs/CONTAINER_SECURITY.md)

### Commit 62: [9b1a831](https://github.com/vig-os/devkit/commit/9b1a831cb8bce171a032498609e6957426f4e2dc) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 08:55 AM
docs(changelog): record the security exception expiry advance notice, 60 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 63: [6632d63](https://github.com/vig-os/devkit/commit/6632d631eb6ad0cdac93e79a28d4e4ffae9b171c) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 09:19 AM
chore(security): stagger exception expiries onto distinct Wednesdays (#1554), 137 files modified (.github/dependency-review-allow.txt, .trivyignore, .vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 64: [e4726f1](https://github.com/vig-os/devkit/commit/e4726f1dbf6a57f8abe7eca64fa621ab546ce03f) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 09:19 AM
feat(vigutils): warn seven days before a security exception expires (#1555), 718 files modified

### Commit 65: [cdf8eb1](https://github.com/vig-os/devkit/commit/cdf8eb107666c916ec1c5ff68474b4e06e1c97f0) by [renovate[bot]](https://github.com/apps/renovate) on August 20, 2026 at 10:59 AM
build(pip): lock file maintenance, 6 files modified (uv.lock)

### Commit 66: [88aee96](https://github.com/vig-os/devkit/commit/88aee96a818ce9b2abbda53403634964551e08e0) by [c-vigo](https://github.com/c-vigo) on August 20, 2026 at 11:10 AM
build(pip): lock file maintenance (#1556), 6 files modified (uv.lock)

### Commit 67: [ec24707](https://github.com/vig-os/devkit/commit/ec2470763b5a925c317120a5c5ef7e7ab34b2ae6) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 20, 2026 at 11:17 AM
chore: freeze changelog for release 1.11.0, 22 files modified (CHANGELOG.md)

### Commit 68: [ae4bbb0](https://github.com/vig-os/devkit/commit/ae4bbb09f15bd3013347c89e4c6b9b4e9bd1f303) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 20, 2026 at 11:18 AM
chore: sync workspace manifest for release 1.11.0, 22 files modified (assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 69: [b30ce48](https://github.com/vig-os/devkit/commit/b30ce4883bec8b038d5c4000285f12ac4ecd3c8f) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 20, 2026 at 12:21 PM
chore: finalize release 1.11.0, 8 files modified (.vig-os, CHANGELOG.md, README.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 70: [326e7e0](https://github.com/vig-os/devkit/commit/326e7e0695460919f3867a284732080a945493e4) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 20, 2026 at 12:22 PM
chore: sync issues and PRs, 1891 files modified
