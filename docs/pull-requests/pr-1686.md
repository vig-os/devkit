---
type: pull_request
state: closed (merged)
branch: bugfix/1668-unbound-1261-cve-batch → main
created: 2026-09-25T06:04:06Z
updated: 2026-09-25T06:23:48Z
author: vig-os-release-app[bot]
author_url: https://github.com/vig-os-release-app[bot]
url: https://github.com/vig-os/devkit/pull/1686
comments: 1
labels: release-neutral
assignees: none
milestone: none
projects: none
merged: 2026-09-25T06:23:46Z
synced: 2026-09-25T07:33:04.910Z
---

# [PR 1686](https://github.com/vig-os/devkit/pull/1686) fix(security): except the second unbound 1.26.1 CVE batch in the vulnix register

## Release-neutral change to `main`

Carries a change that alters **no published artifact**, so it needs no release.

Cutting a release for this would republish a functionally identical image and hand every consumer an adoption PR that bumps a pin and changes nothing they run.

### What must hold before this merges

The `Release-Neutral Guard` check proves it and posts a verdict comment:

- **1** — no release content (`CHANGELOG.md`, its mirror, `.vig-os`) in the diff
- **2** — `devShells.default`, `devkitImage` and `devkitImageEnv` derivations byte-identical to `main` *(the contract)*
- **3** — the consumer scaffold under `assets/` is untouched
- **4** — `main`'s `## Unreleased` is still empty
- **5** — no release train is in flight

Gate 2 is the one that matters: equal derivation paths mean the published artifacts *cannot* differ, whatever the diff touched.

### Review note

Opened by the release App so the maintainer's approval is permitted — GitHub forbids authors approving their own PRs, and `main` requires one review with no App bypass.

Do not push to this branch after approving: `main` sets `dismiss_stale_reviews_on_push`.

Dispatched from https://github.com/vig-os/devkit/actions/runs/36101199123

Refs: #1668, #1669 


---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/devkit/pull/1686#issuecomment-5827722812) by [@github-actions[bot]](https://github.com/apps/github-actions)

_Posted on September 25, 2026 at 06:09 AM_

### Release-neutral guard — verdict

**This change is release-neutral.** Every published derivation is byte-identical to `main`, so merging it changes nothing any consumer receives and no release is required.

| Gate | Proved |
|------|--------|
| 1 | No release content (`.vig-os`, which carries `DEVKIT_VERSION`) touched |
| 2 | `devShells.default`, `devkitImage`, `devkitImageEnv` derivations unchanged, compared with the changelog normalized |
| 3 | Consumer scaffold under `assets/` byte-identical |
| 5 | No release train in flight |

**Files carried to `main`:**
- `.vulnixignore`
- `CHANGELOG.md`
- `assets/workspace/.devcontainer/CHANGELOG.md`

**Changelog drift — the one published difference.** This PR edits `CHANGELOG.md` (and its `assets/workspace/.devcontainer/` mirror), which is baked into the image. Gate 2 therefore **normalized it away**: both sides were evaluated with `main`'s changelog, so the identity proved above covers everything *except* the changelog text.

That is allowed by design — `main` may carry changes it has landed but not shipped, and `## Unreleased` is where they are described (superseding [#590](https://github.com/vig-os/devkit/issues/590)). `sync-main-to-dev.yml` fires on `push: [main]`, so the entry reaches `dev` on its own and the next train freezes it normally. Read the entry before approving:

```diff
diff --git a/CHANGELOG.md b/CHANGELOG.md
index 50612882..b7f4e4b4 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -49,6 +49,27 @@ and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0
 
 ### Security
 
+- **Except the second unbound 1.26.1 CVE batch in the vulnix register**
+  ([#1668](https://github.com/vig-os/devkit/issues/1668),
+  [#1669](https://github.com/vig-os/devkit/issues/1669))
+  - The 2026-09-24 nightly went red on both lanes on the same package as the
+    day before, with two CVEs the 2026-09-23 exception did not carry:
+    `CVE-2026-82717` (9.8) and `CVE-2026-81634` (7.5)
+  - Not new upstream work and not a closure change: all three unbound CVEs
+    were published 2026-09-16 and are fixed by the same 1.26.1 release. They
+    arrived a day apart because vulnix matches on CPE and NVD analysed the
+    two additions at `2026-09-23T19:50Z`/`19:51Z` — after that day's scan ran
+  - Both are resolver paths (RRSet canonicalisation; CNAME synthesis on an
+    upstream response), so the existing reachability finding covers them
+    unchanged: the closure carries `libunbound` only, with no daemon and no
+    `unbound` binary
+  - Added to the existing `2026-11-04` block rather than a new one — all three
+    share one death condition, the pin advance that ships unbound 1.26.1, and
+    must be deleted together. The date is deliberately unchanged
+  - The remediation lever shortened in the meantime: the `staging-26.05`
+    backport has merged, leaving the fix one `staging` -> `nixos-26.05` cycle
+    from the pinned channel rather than two branch hops
+
 ## [1.16.0](https://github.com/vig-os/devkit/releases/tag/1.16.0) - 2026-09-23
 
 ### Added
```

> This PR amends `.vulnixignore`. The substantive record of the risk acceptance is the register block itself, which reaches `main` with this merge; the changelog entry above is its release note — 10 of the last 12 register commits carried one, so do not let this be the exception.

> Do not push to this branch after approving — `main` sets `dismiss_stale_reviews_on_push`.


---
---

## Commits

### Commit 1: [b130313](https://github.com/vig-os/devkit/commit/b130313736e340e851c767844bdca6a0dc13d2a9) by [c-vigo](https://github.com/c-vigo) on September 24, 2026 at 11:18 AM
fix(security): except the second unbound 1.26.1 CVE batch in the vulnix register, 103 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 2: [1870938](https://github.com/vig-os/devkit/commit/1870938b22626192e757fd344ee2b9e37320d9e4) by [c-vigo](https://github.com/c-vigo) on September 25, 2026 at 06:05 AM
Merge branch 'main' into bugfix/1668-unbound-1261-cve-batch, 1239 files modified
