---
type: pull_request
state: closed (merged)
branch: bugfix/1689-prepare-changelog-loose-bullets → dev
created: 2026-09-25T07:45:01Z
updated: 2026-09-25T08:00:57Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devkit/pull/1691
comments: 0
labels: bug, area:ci
assignees: none
milestone: none
projects: none
merged: 2026-09-25T08:00:55Z
synced: 2026-09-26T07:27:10.510Z
---

# [PR 1691](https://github.com/vig-os/devkit/pull/1691) fix(changelog): make prepare-changelog fail closed on content it cannot freeze

## Description

`prepare-changelog validate` and `prepare-changelog prepare` disagreed about what counts as content under `## Unreleased`, and the gap destroyed entries silently. `validate` accepted any line starting with `-`; `prepare` froze only bullets under a recognised `### <Standard>` subsection. A bullet written directly under the heading therefore passed validation, and `prepare` then wrote the new version section empty, dropped the bullet, and exited 0 with only a warning. The same body regex was also unbounded on a bare `## Unreleased`, so the previous release's bullets were parsed as unreleased content and duplicated into the new section. Since #1682 the hotfix classifier selects `prepare` by calling `validate`, which made both paths live.

This PR gives both commands one bounded body and one notion of content, and makes `prepare` fail closed: it never exits 0 having removed a bullet from `## Unreleased` or from the folded `## [X.Y.Z]` block.

## Type of Change

- [x] `fix` -- Bug fix
- [x] `test` -- Adding or updating tests
- [x] `docs` -- Documentation only

### Modifiers

- [ ] Breaking change (`!`)

## Changes Made

- `_unreleased_body()` bounds the Unreleased capture at the next top-level `## ` heading, so a bare section can no longer swallow the release below it.
- `validate` now reports "has content" only when `_parse_subsections` finds something, i.e. exactly when `prepare` can freeze it. Loose bullets (outside a recognised `### ` subsection) and duplicated standard headings are refused with exit 1 and a message naming the offending lines or headings.
- `prepare` and `seed` share that guard through `extract_unreleased_content`, and `prepare` refuses when there is nothing to freeze instead of writing an empty section. The check runs after `_pop_version_section`, so the #612 reused-release-branch fold still works.
- The same guard covers the popped `## [X.Y.Z] - TBD` block, and `validate --version` (the release-time gate) now uses `_parse_subsections` too, so it can no longer green-light a section a later `prepare` would empty.
- Duplicate headings are refused rather than merged: a repeated `### Added` is the signature of a mis-resolved changelog conflict, and stitching it back together would hide that from the author.
- No workflow logic changes. The #1682 classifier treats any non-zero `validate` as "seed", and `seed` now refuses the same input, so the hotfix lane fails closed on its dry probe. Both `prepare-hotfix.yml` copies only had stale comments corrected; `docs/RELEASE_CYCLE.md` and `docs/DOWNSTREAM_RELEASE.md` updated to match (the "never call prepare on an empty Unreleased, it scoops the previous release" claim is retired because both halves are now false).
- A regression guard pins that `synthesize-bot-changelog`'s `#### Dependencies` block under `### Changed` still counts as content.

Out of scope, pre-existing and unchanged: when `## Unreleased` is not the first `## ` section, content above it is still discarded by `prepare`. The new docstring says so explicitly rather than claiming otherwise.

## Changelog Entry

### Fixed

- **`prepare-changelog prepare` no longer deletes entries it cannot freeze**
  ([#1689](https://github.com/vig-os/devkit/issues/1689))
  - `validate` counted any line starting with `-` under `## Unreleased` as
    content, while `prepare` only moved bullets sitting under a recognised
    `### <section>` heading. A bullet written straight under `## Unreleased`
    therefore passed `validate`, and `prepare` then wrote an **empty**
    `## [X.Y.Z] - TBD`, reset `## Unreleased`, dropped the bullet and exited
    **0** with a warning. #1682's classifier picks its mode by calling
    `validate`, so it selected `prepare` on exactly the input `prepare` could
    not freeze.
  - A bare `## Unreleased` (no `###` headings at all) was worse: the body
    capture was bounded by `\n## [`, a lookahead that cannot fire when the
    separating newline is already consumed by the heading match, so the capture
    ran to end of file and **the previous release's entries were read as
    unreleased content** — `validate` reported content and `prepare`
    duplicated that release into the new version section.
  - Both commands now read one body bounded at the next `##` heading and share
    one notion of content: "has content" means "`prepare` can freeze this".
    Bullets outside a recognised `###` subsection are refused by `validate`,
    `prepare` and `seed` alike, naming every offending line and leaving the file
    untouched; `prepare` refuses when there is nothing to freeze instead of
    writing an empty section. The hotfix lane needs no change — `validate`
    exits 1, the classifier picks `seed`, and `seed` refuses the same input, so
    the lane fails closed instead of losing the entry.
  - The same guard now runs over the `## [X.Y.Z]` block `prepare` folds back in
    on a reused release branch, closing the twin of the bug: a bullet written
    straight under the version heading — the shape the hotfix runbook asks
    authors to fill in — was dropped just as silently. `validate --version`,
    the release-time gate in `release.yml` and the scaffold's
    `release-core.yml`, reads content the same way, so it no longer blesses a
    section `prepare` would empty on the next cycle.
  - A **repeated** standard heading (`### Added` twice, the normal result of a
    hand-resolved merge conflict) is refused as well. Only the first block of
    each heading was ever read, so the rest was deleted at exit 0. It refuses
    rather than merging the blocks: a repeated heading means an edit went
    wrong, and quietly stitching it back together would hide that.

## Testing

- [x] Tests pass locally (`just test`)
- [x] Manual testing performed (describe below)

```
PYTHONPATH=$PWD/packages/vig-utils/src uv run pytest packages/vig-utils/tests \
  tests/test_workflow_prepare_hotfix.py tests/test_release_time_changelog.py \
  tests/test_workflow_release_neutral.py -q      # 698 passed, exit 0
just precommit                                     # 53 hooks passed, exit 0
```

RED-first at both test commits: 17 failed / 142 passed at `b1c9dfb9` against the pre-fix source, 11 failed / 161 passed at `f93f9cf7`.

### Manual Testing Details

CLI runs on fixture changelogs, before and after: loose bullet under `## Unreleased` (was: validate 0, prepare 0, bullet deleted; now: both exit 1, file byte-identical); bare `## Unreleased` above a release (was: validate 0, prepare duplicated the release; now: validate 1, prepare refuses, seed succeeds); loose bullet inside `## [1.0.1] - TBD` (was: deleted at exit 0; now: prepare and `validate --version` exit 1); duplicate `### Added` (was: second block deleted; now: refused naming the heading). The real devkit `CHANGELOG.md` still passes `validate`, `prepare 9.9.9` and `validate --version 9.9.9`.

Local note: the nix devshell's `PYTHONPATH` puts a previously built `vig-utils` ahead of the editable install for subprocesses, so the CLI subprocess tests need the prefix shown above locally. CI builds the devshell from the branch.

Not exercised: a live `prepare-hotfix.yml` or `release.yml` dispatch. The fail-closed path is proven by unit and subprocess tests only.

## Checklist

- [x] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [x] I have commented my code, particularly in hard-to-understand areas
- [x] I have updated the documentation accordingly
- [x] I have updated `CHANGELOG.md` in the `[Unreleased]` section (and pasted the entry above)
- [x] My changes generate no new warnings or errors
- [x] I have added tests that prove my fix is effective or that my feature works
- [x] New and existing unit tests pass locally with my changes
- [x] Any dependent changes have been merged and published

## Additional Notes

The issue text originally called the bare-Unreleased duplication "masked by the classifier"; a follow-up comment on #1689 corrects that (the old `validate` also exited 0 there), and this PR fixes both.

Refs: #1689



---
---

## Commits

### Commit 1: [b1c9dfb](https://github.com/vig-os/devkit/commit/b1c9dfb9e353b0de3326f460304ecd93192fc071) by [c-vigo](https://github.com/c-vigo) on September 25, 2026 at 07:15 AM
test(changelog): cover loose bullets and bare Unreleased in prepare, 217 files modified (packages/vig-utils/tests/test_prepare_changelog.py)

### Commit 2: [6558a93](https://github.com/vig-os/devkit/commit/6558a9329e5d7a46411cc5a9153c9d3e3618864a) by [c-vigo](https://github.com/c-vigo) on September 25, 2026 at 07:18 AM
fix(changelog): make prepare fail closed on unfreezable Unreleased content, 114 files modified (packages/vig-utils/src/vig_utils/prepare_changelog.py)

### Commit 3: [74b1f00](https://github.com/vig-os/devkit/commit/74b1f0044913eb275e1c92429cc0312bcd9f6610) by [c-vigo](https://github.com/c-vigo) on September 25, 2026 at 07:20 AM
docs(changelog): record the fail-closed prepare guard, 50 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 4: [58eda52](https://github.com/vig-os/devkit/commit/58eda523f9396668542f3232687b5f1f2a54a188) by [c-vigo](https://github.com/c-vigo) on September 25, 2026 at 07:22 AM
docs(changelog): unmangle the escape in the Unreleased body docstring, 7 files modified (packages/vig-utils/src/vig_utils/prepare_changelog.py)

### Commit 5: [f93f9cf](https://github.com/vig-os/devkit/commit/f93f9cf7eb97539e266d9b3428b52192f7135317) by [c-vigo](https://github.com/c-vigo) on September 25, 2026 at 07:37 AM
test(changelog): cover the version-block twin and duplicate headings, 214 files modified (packages/vig-utils/tests/test_prepare_changelog.py)

### Commit 6: [1b3aa94](https://github.com/vig-os/devkit/commit/1b3aa94919b49fafd3fc58a91bd0671685e8e4c0) by [c-vigo](https://github.com/c-vigo) on September 25, 2026 at 07:39 AM
fix(changelog): extend the fail-closed guard to the version block, 82 files modified (packages/vig-utils/src/vig_utils/prepare_changelog.py)

### Commit 7: [c39f686](https://github.com/vig-os/devkit/commit/c39f68666329c1cf525c61704e79493e891d2fd6) by [c-vigo](https://github.com/c-vigo) on September 25, 2026 at 07:41 AM
docs(release): retire the "prepare scoops the previous release" claim, 56 files modified
