---
type: pull_request
state: closed (merged)
branch: release/1.7.0 → main
created: 2026-08-07T16:51:14Z
updated: 2026-08-08T08:49:09Z
author: vig-os-release-app[bot]
author_url: https://github.com/vig-os-release-app[bot]
url: https://github.com/vig-os/devkit/pull/1385
comments: 0
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-08-08T08:49:07Z
synced: 2026-08-09T03:48:39.610Z
---

# [PR 1385](https://github.com/vig-os/devkit/pull/1385) chore: release 1.7.0

# [Release 1.7.0](https://github.com/vig-os/devkit/releases/tag/1.7.0) - 2026-08-07

This PR prepares release 1.7.0 for merge to main.

## [1.7.0](https://github.com/vig-os/devkit/releases/tag/1.7.0) - 2026-08-07

### Added

- **`CODE_OF_CONDUCT.md`** ([#1372](https://github.com/vig-os/devkit/issues/1372))
  - Contributor Covenant 2.1 at the repository root. Enforcement reports go
    through the same [GitHub Private Vulnerability Reporting](https://github.com/vig-os/devkit/security/advisories/new)
    channel `SECURITY.md` already designates, so there is one private route to
    the maintainers rather than two.

### Changed

- **Stamped workflows authenticate with App Client IDs — new org secret required** ([#1365](https://github.com/vig-os/devkit/issues/1365))
  - **Action required before upgrading:** create a `DEVKIT_UPGRADE_APP_CLIENT_ID`
    secret (org or repo) holding the upgrade App's Client ID (`Iv23li…`).
    **Create the secret first, then upgrade** — config-first, always. For this
    release the legacy numeric `DEVKIT_UPGRADE_APP_ID` still works as a
    fallback (with a deprecation warning), so an upgrade that races the secret
    is not bricked; the fallback is removed in a future release
    ([#1366](https://github.com/vig-os/devkit/issues/1366)).
  - `sync-issues.yml` now passes `COMMIT_APP_CLIENT_ID` through the `client-id`
    input added in `vig-os/sync-issues-action` v0.5.0 — the numeric
    `COMMIT_APP_ID` secret is no longer read anywhere and can be retired once
    every consumer is on this release.
  - `devkit-upgrade.yml` prefers `DEVKIT_UPGRADE_APP_CLIENT_ID` in its
    preflight gate and `create-github-app-token` mint.
  - Nothing in the auth path is numerically load-bearing: GitHub accepts either
    the App ID or the Client ID as the App JWT issuer.
- **`CONTRIBUTE.md` renamed to `CONTRIBUTING.md`** ([#1372](https://github.com/vig-os/devkit/issues/1372))
  - GitHub's community profile only recognises `CONTRIBUTING.md`, so the guide
    was invisible to it and no "Contributing guidelines" link appeared on new
    issues and pull requests.
  - The file is generated, so the rename covers the whole chain:
    `docs/templates/CONTRIBUTE.md.j2` -> `docs/templates/CONTRIBUTING.md.j2`,
    `docs/generate.py`, the pymarkdown exclude in `.pre-commit-config.yaml`, the
    `expected_doc` guard in `release.yml`, the `README.md` link (and its
    template), `docs/NIX.md`, and the `docs.yml` issue template. Historical
    records — released changelog entries, `docs/issues/`, `docs/pull-requests/`
    — keep the old name.
- **Renovate: update `github/codeql-action` from `d1ba80a` to `5595cca`** ([#1367](https://github.com/vig-os/devkit/pull/1367))
- **Renovate dependency update** ([#1368](https://github.com/vig-os/devkit/pull/1368))
  - Update `actions/attest` from `v4.2.1` to `v4.2.2`
  - Update `actions/attest-build-provenance` from `v4.1.1` to `v4.2.2`
  - Update `vig-os/commit-action` from `v0.3.1` to `v0.3.2`
  - Update `vig-os/sync-issues-action` from `v0.4.0` to `v0.5.0`

### Fixed

- **Floating tags move via `git push` — first release of a new level no longer fails** ([#1377](https://github.com/vig-os/devkit/issues/1377))
  - The scaffolded `promote-release.yml` created brand-new floating levels via
    `POST /git/refs`, the one ref-mutation path where GitHub does not honor the
    Release App's Integration ruleset bypass for the `creation` rule — so the
    first release of every new `<prefix>X` / `<prefix>X.Y` level died with the
    opaque `Reference does not exist` (HTTP 422) despite the App holding an
    `always` bypass on the whole Tag ruleset (hit on `commit-action` 0.3.1 and
    `sync-issues-action` 0.5.0).
  - `move_tag()` now force-pushes `TARGET_SHA:refs/tags/<name>` over git with
    the App installation token plumbed explicitly into the push URL (checkout's
    persisted credentials are the default `github.token`, which has no bypass)
    — the exact path `release-publish.yml` already uses to create release tags,
    empirically bypassed fine. Create and move collapse into one branch-free
    path; the idempotence read-and-skip guard and the loud `::error` +
    MIGRATION.md fallback from #1158 stay, with the moot "grant a creation
    bypass" remediation rewritten (the bypass already exists — the REST create
    path simply ignores it).
- **Flake pymarkdown excludes follow the `CONTRIBUTING.md` rename** ([#1380](https://github.com/vig-os/devkit/issues/1380))
  - `nix/hooks.nix` (runner, check, and consumer profiles) and the scaffold
    template `assets/workspace/.pre-commit-config.yaml` still excluded the
    removed `CONTRIBUTE.md`, drifting from the root `.pre-commit-config.yaml`
    updated in [#1372](https://github.com/vig-os/devkit/issues/1372); the
    renamed contributing guide was no longer excluded from pymarkdown.
- **A no-diff devkit upgrade no longer strands its adoption issue** ([#1347](https://github.com/vig-os/devkit/issues/1347))
  - The scaffolded `devkit-upgrade.yml` opens the adoption issue *before*
    `install.sh --force` runs (the branch name embeds its number and the
    in-shell commit needs the `Refs:` line), so a `workflow_dispatch` against an
    already-current consumer created an issue, found zero diff, skipped publish
    and PR, and exited green — leaving an issue open with no PR ever attached.
    Every credential probe or repair run against a current consumer left one
    behind.
  - The find-or-create step now reports which branch it took, and a final
    cleanup step on the no-diff path **closes an issue this run created** (with
    a "no diff at `<version>`" comment) while **leaving a reused one open** — a
    mid-train `rc1 → rc2 → final` issue can legitimately see a no-op bump, and
    auto-closing it would be wrong. Comment only in that case.
- **Upgrades prune scaffold paths retired since the consumer's pin** ([#1348](https://github.com/vig-os/devkit/issues/1348))
  - `install.sh --force` regenerated what the *current* scaffold manages and
    pruned what the current mode / workflow model / feature set excludes — but a
    path an **old** devkit shipped and a later one **retired** was claimed by
    neither and rode along through every upgrade. Going 0.3.4 → 1.6.0 preserved
    `.cursor/`, `.github/actions/resolve-image/`,
    `.github/workflows/renovate-changelog.yml` and `.hadolint.yaml`. The
    workflow file is the sharp one: left in place it is live, duplicates the
    `renovate-changelog-build`/`-commit` pair that replaced it, and calls the
    pruned `resolve-image` action — so it breaks at its next trigger rather
    than at upgrade time.
  - A cumulative retired-paths manifest (version → paths that version stopped
    shipping) is now consulted against the pin `.vig-os` carried **before** the
    run — the legacy `DEVCONTAINER_VERSION` key included, since repos still on
    it are precisely the oldest. Each prune is announced on stdout and listed
    under `DELETED` in the `--force --preview` report.
  - The prune is gated on that pin: a repo pinned at or past the retiring
    version was never shipped the path, so an identically named `.cursor/` or
    `.hadolint.yaml` there is the consumer's own and is left untouched. A repo
    that already upgraded past a retirement before this fix keeps its
    leftovers — a one-time manual cleanup, documented in `docs/MIGRATION.md`.
- **Preserved-file diffs show template content, not a symlink typechange** ([#1349](https://github.com/vig-os/devkit/issues/1349))
  - A `--force` upgrade prints the divergence between each preserved consumer
    file and the incoming template so the consumer can fold in template
    evolution deliberately. In the shipped image the devkit assets are
    `/nix/store` symlinks, so `git diff --no-index` compared the *link* against
    the consumer's regular file and rendered a typechange — "symlink deleted /
    file added", with the store path as its only line — instead of the content
    diff. Seen on the `vig-os/scitadel` 0.3.3 → 1.6.0 migration.
  - The template side is now dereferenced before the comparison, so the hunk
    shows the actual template changes. A template symlink whose target does not
    resolve is still treated as a missing template (no diff, no error), and the
    diff header keeps naming the file rather than an opaque store path.
- **CI dev-shell PATH keeps its priority order** ([#1351](https://github.com/vig-os/devkit/issues/1351))
  - The direnv-mode `setup-devkit-toolchain` wrote the dev-shell's `/nix/store`
    bin dirs to `GITHUB_PATH` highest-priority-first, but the runner *prepends*
    every line in file order — so the following steps ran with the dev-shell
    PATH exactly reversed. The dirs are now written reversed, and the per-line
    prepend rebuilds the dev-shell order verbatim.
  - Only multi-store-path toolchains were affected: with one tool per store
    path the order is unobservable. A stdenv toolchain is not — the raw `gcc`
    shadowed `gcc-wrapper` (likewise `binutils`) and C builds failed with the
    unwrapped-compiler signature `ld.bfd: cannot find Scrt1.o` /
    `cannot find -lc`, while the same build stayed green locally under
    `nix develop` (first hit: `vig-os/h5v`'s vendored HDF5 build).
- **shellHook env forward no longer ships `UV_PYTHON` to CI** ([#1353](https://github.com/vig-os/devkit/issues/1353))
  - Two correct mechanisms in the direnv-mode `setup-devkit-toolchain` cancelled
    out. The step drops the bare Nix CPython from the exported `PATH` and
    forwards `UV_PYTHON_DOWNLOADS_JSON_URL` so `uv sync` builds the runner venv
    from a downloaded manylinux CPython (#698/#703/#729) — but the shellHook env
    forward (#1180) diffs the dev-shell env against the ambient one and shipped
    every added var, `mkProjectShell`'s `UV_PYTHON` store-path pin included.
    `UV_PYTHON` beats PATH resolution, so the venv was built on the Nix
    interpreter regardless and any C-extension wheel then failed under the Nix
    loader on Ubuntu with `ImportError: libstdc++.so.6: cannot open shared
    object file`. Existing direnv Python consumers were green only because their
    test dependencies are pure-Python; first live hit
    `exo-pet/playground-carlos#9` (numpy 2.4.4).
  - `UV_PYTHON` and `UV_PYTHON_DOWNLOADS` are now denied by name in that
    forward's denylist. Both are Nix-host-only by construction, and the second
    is required for the first: on its own, a forwarded
    `UV_PYTHON_DOWNLOADS=never` would forbid the very managed-CPython download
    the `_JSON_URL` forward exists to enable, turning the wheel `ImportError`
    into "no interpreter found". The patterns are exact, so the deliberate
    `UV_PYTHON_DOWNLOADS_JSON_URL` forward is untouched — now pinned by a test.
  - Audit of the other vars `mkProjectShell` puts in the dev-shell env: kept
    `BATS_LIB_PATH` (a store path, but the store is realized on the runner and
    `bats` runs from the dev-shell `PATH` there), `NVIM_APPNAME` (portable
    config) and `UV_PYTHON_DOWNLOADS_JSON_URL`. `LD_LIBRARY_PATH` is exported
    only behind the shellHook's own `/etc/NIXOS` guard, so it never reaches a
    hosted runner and is the correct value on a NixOS self-hosted one — left
    forwardable. Nothing else the builder sets is host-specific.
- **shellHook env forward stops leaking Nix-host state** ([#1358](https://github.com/vig-os/devkit/issues/1358))
  - `PYTHONPATH` is now forwarded **component-wise**: the direnv-mode preamble
    splits it on `:`, drops every `/nix/store` entry and forwards what remains,
    skipping the var when nothing is left. With `python` in the dev-shell
    `packages` the nixpkgs setup hook fills `PYTHONPATH` with store
    site-packages dirs; forwarded whole, those Nix-built `cp3xx` packages joined
    the `sys.path` of the *downloaded* manylinux CPython — same ABI tag,
    importable, the same mixed-loader shape as #1353's
    `ImportError: libstdc++.so.6`. A blanket deny was not an option: a
    `shellHook` exporting `PYTHONPATH=$PWD/src` is a legitimate #1180 use case
    and still arrives on CI intact.
  - The denylist gained the stdenv machinery it was still missing —
    `_PYTHON_HOST_PLATFORM`, `_PYTHON_SYSCONFIGDATA_NAME`, `DETERMINISTIC_BUILD`,
    `CONFIG_SHELL`, the `do[A-Z]*` build flags (`doCheck`, `doInstallCheck`,
    `doDist`; the symmetric partner of the already-denied `dont*`) — and the
    stdenv cc/binutils hook names `AR AS CC CXX LD NM OBJCOPY OBJDUMP RANLIB
    READELF SIZE STRINGS STRIP`. stdenv sets those toolchain names in **every**
    dev shell as bare commands (`CC=gcc`), so every direnv consumer was shipping
    them to its whole CI job. Inside dev-shell context the wrapped toolchain
    already wins via `PATH` after #1351; for a step that runs outside it
    (`setup-python` plus `pip` building a C extension) a forwarded `CC`
    resolving to the Nix wrapper recreates the #1353 mixed-toolchain hazard.
    This deliberately supersedes the `shellHook` `CC`/`CXX` forward that
    vig-os/h5v used pre-#1351 — #1351 removed the need for it. The `native`
    module's generic `CC=cc`/`CXX=c++` exports likewise no longer reach CI; they
    exist so build backends fall back to `PATH` discovery (#879/#893), which the
    forwarded dev-shell `PATH` still provides. A CI step that needs a specific
    compiler should set it in the workflow. Patterns are case-sensitive, so
    `DOCKER_*`/`DO_SOMETHING` are unaffected.
  - `IN_NIX_SHELL` is denied too: forwarding `IN_NIX_SHELL=impure` made every
    subsequent CI step claim to be running inside a nix shell.
  - The related NixOS-self-hosted-runner gap in the CPython `PATH` filter is
    tracked separately in
    [#1360](https://github.com/vig-os/devkit/issues/1360).
- **Python consumers work on NixOS self-hosted runners** ([#1360](https://github.com/vig-os/devkit/issues/1360))
  - The direnv-mode preamble dropped the bare Nix CPython from the exported
    `PATH` whenever `pyproject.toml` was present — unconditionally. That is
    right on a hosted FHS runner, where `uv` downloads a managed manylinux
    CPython (#698/#703/#729), and wrong by construction on a **NixOS**
    self-hosted runner, where such a download cannot execute at all. #1353 made
    the gap reachable: the `UV_PYTHON` pin it stopped forwarding was what had
    accidentally masked it. No consumer matched the combination yet (the one
    NixOS-runner consumer has no `pyproject.toml`), so this was a latent
    landmine rather than a live break.
  - The step now probes the runner's own OS via `/etc/NIXOS` — the same marker
    `mkProjectShell`'s `shellHook` uses to gate `LD_LIBRARY_PATH` — and takes
    the matching branch. On a NixOS runner the `pyproject.toml`-gated CPython
    exclusion is skipped, so the store interpreter stays on `PATH`, **and**
    `mkProjectShell`'s `UV_PYTHON` / `UV_PYTHON_DOWNLOADS=never` pins are
    forwarded instead of denied. Both halves are needed: `PATH` alone leaves
    `uv`'s python-preference free to prefer a managed download, and the pins
    are what make it use the Nix interpreter deterministically. The podman
    exclusion stays unconditional, and `UV_PYTHON_DOWNLOADS_JSON_URL` is still
    forwarded (harmless once the pin wins).
  - On every non-NixOS runner the behavior is byte-identical to before,
    #1353's denial of the same two pins included.
- **Prefixed-tag releases publish their changelog notes** ([#1355](https://github.com/vig-os/devkit/issues/1355))
  - In a consumer that sets `DEVKIT_TAG_PREFIX`, the release pipeline wrote one
    changelog heading and read back another. `release-core.yml` finalizes with
    `prepare-changelog finalize … --tag-prefix "$TAG_PREFIX"`, which composes the
    prefix into the heading (`## [v1.0.0](…/releases/tag/v1.0.0) - …`), while
    `release-publish.yml` extracted the notes by matching `## [$VERSION]` on the
    *bare* semver — a match that can never land on a prefixed heading.
  - It failed soft: the empty extraction hit the fallback path, so the release
    was published with the literal body `No changelog notes found for X.Y.Z`.
    The tag and the release object were both correct and nothing went red, which
    is why it survived (observed releasing `vig-os/org-config` `v1.0.0`).
  - The extraction step now takes `tag_prefix` and accepts the composed tag
    `## [${TAG_PREFIX}${VERSION}]` alongside the bare one. Both are needed: a
    candidate publishes *before* the finalize step (which is gated on a final
    `release_kind`), so its heading is still the unprefixed `## [X.Y.Z] - TBD` that
    `prepare` wrote — matching only the composed tag would have moved the empty
    notes onto the candidate path. Only prefixed repos were affected; an empty
    prefix makes the two forms the same string.
- **MIGRATION.md env-forward denylist prose replaced by an SSoT pointer** ([#1362](https://github.com/vig-os/devkit/issues/1362))
  - The "direnv mode: `shellHook` environment forwarding" section enumerated the
    denylist by hand and had fallen behind the shipped action: it predated the
    `UV_PYTHON`/`UV_PYTHON_DOWNLOADS` denial (#1353), the stdenv additions and
    the cc/binutils hook names (#1358), and the NixOS-runner exception that
    forwards those uv pins after all (#1360) — and its two-bullet shape could
    not express the `PYTHONPATH` store-component strip, which is a transform
    rather than a deny.
  - The enumeration is replaced by a description of the denied *categories* with
    representative examples, the `PYTHONPATH` transform, and a pointer to the
    `devkit_env_denied` function in the consumer's own
    `.github/actions/setup-devkit-toolchain/action.yml` as the single source of
    truth. The surrounding narrative is unchanged.
- **A cancelled CI job no longer leaves `Test Summary` green** ([#1371](https://github.com/vig-os/devkit/issues/1371))
  - `Test Summary` is the only required status check on `dev`, and it flagged a
    needed job only on `result == "failure"`. A job that was **cancelled** — job
    timeout, concurrency cancel, runner eviction, a manual "Cancel workflow" —
    produced no verdict at all, yet the aggregate printed "All executed test
    suites passed" and exited 0, so a pull request whose CI never finished was
    mergeable.
  - All eight needed jobs now trip the gate on `cancelled` as well as `failure`.
    `skipped` stays tolerated by design: `workflow_dispatch` takes a
    `test-suite` subset input that every job is gated on, and
    `commit-checks`/`dependency-review` are pull-request only.

### Security

- **Release tags are lightweight refs, so no unsigned object remains in the release chain** ([#1370](https://github.com/vig-os/devkit/issues/1370))
  - `release.yml` created the tag with `git tag -a` under
    `vigOS Release Bot <release@vig-os.local>`, producing an **annotated tag
    object** whose tagger was permanently unverifiable — live tag `1.6.0`
    reports `verification.reason: "unsigned"`. Signing it was never reachable:
    the tag is written under the Release **App** installation token and a GitHub
    App has no registrable GPG/SSH key, while the server-side `POST /git/tags`
    route stores the payload verbatim without signing it either.
  - The publish job now creates the tag as a plain `refs/tags/<version>` ref at
    the release commit (`POST /git/refs`). A lightweight tag has no tagger and
    no payload, so there is nothing left that can report `unsigned`, and the
    commit the ref resolves to is the GitHub-verified release commit. The
    candidate-collision guard and the "already tagged at the finalize SHA"
    skip are unchanged; the now-unused `git-user-name` / `git-user-email`
    dispatch inputs are removed from `release.yml` (`prepare-release.yml`, which
    actually writes commits, keeps its own).
- **Consumer release tags are lightweight refs too — the scaffold stops publishing unsigned tag objects** ([#1378](https://github.com/vig-os/devkit/issues/1378))
  - The scaffold's `release-publish.yml` still created every consumer's
    `X.Y.Z` tag with `git tag -a` under a GitHub App identity — an **annotated
    tag object** reporting `verification.reason: "unsigned"` forever, for the
    same unreachable-signature reasons as [#1370](https://github.com/vig-os/devkit/issues/1370).
    The scaffold was internally inconsistent: `promote-release.yml` already
    creates the *floating* tags (`vX`, `vX.Y`) as lightweight refs, while the
    version tag — the one users pin and audit — was an unsigned object.
  - The publish step now POSTs `refs/tags/<prefix><version>` straight at the
    release commit (`POST /git/refs`). Preserved behaviours: the
    `tag_already_exists` skip, the create-race verification (a lost race is
    accepted only when the existing ref already resolves to the release
    commit), `TAG_PREFIX` composition ([#1044](https://github.com/vig-os/devkit/issues/1044)),
    and the tombstone diagnosis ([#1319](https://github.com/vig-os/devkit/issues/1319))
    — its signature match re-derived against the `POST /git/refs` HTTP 422
    rule-violation error shape.
  - The dead `git_user_name` / `git_user_email` `workflow_call` inputs are
    removed from `release-publish.yml` and their pass-throughs from the
    scaffold `release.yml` publish call; the scaffold's `git-user-name` /
    `git-user-email` **dispatch** inputs stay — the rollback job still writes
    commits with that identity.
  - `promote-release.yml` and `docs/MIGRATION.md` keep the annotated-tag peel
    as backward compatibility for tags published by older scaffolds, with
    corrected wording. Existing annotated tags are left as they are
    (forward-fix policy); the change reaches consumers on their next devkit
    upgrade.
- **Smoke-test orchestration gates the final release on a human approval instead of self-approving** ([#1391](https://github.com/vig-os/devkit/issues/1391))
  - The dispatch listener approved its own release PR with the workflow token,
    which the org now forbids (vig-os/org-config#122 sets
    `can_approve_pull_request_reviews: false` org-wide) — the 1.7.0-rc3 smoke
    chain died at that step.
  - The approve step is replaced by a kind-aware gate: candidate dispatches
    leave the PR unapproved (approval gates the final release only, matching
    the deferred-approval model of [#902](https://github.com/vig-os/devkit/issues/902)),
    while the final dispatch polls up to 30 minutes for a human to approve the
    freshly created release PR before triggering the final release workflow,
    and fails with re-run guidance if nobody does. Every dispatch still
    recreates the smoke release branch and PR from scratch, by design.
- **vulnix register: except CVE-2026-66032, completing the libssh2 malicious-server batch** ([#1386](https://github.com/vig-os/devkit/issues/1386))
  - The 1.7.0-rc1 Vulnix CVE Gate blocked on CVE-2026-66032 (CVSS 8.8, a
    double-free in libssh2's `sftp_open()`), the fourth member of the upstream
    batch whose siblings CVE-2026-66033/66034/66035 were already excepted in
    the 2026-09-02 block ([#1327](https://github.com/vig-os/devkit/issues/1327)) —
    it was absent from the findings when that block was triaged on 2026-08-04
    and surfaced in the feed later.
  - Same closure provenance and reachability as its siblings: libssh2 enters
    the image only as curl's scp/sftp backend, and the flaw requires an
    authenticated SFTP session against a malicious SSH server. Excepted in the
    same 2026-09-02 block; the whole block drops on a nixpkgs rev-advance once
    the merged upstream patches (NixOS/nixpkgs#547491) reach the pinned
    nixos-26.05 channel via the still-open staging-26.05 backport.



---
---

## Commits

### Commit 1: [d9671cb](https://github.com/vig-os/devkit/commit/d9671cb150eb14fcb2bbb639d1db456f391129a1) by [vig-os-release-app[bot]](https://github.com/apps/vig-os-release-app) on August 4, 2026 at 01:37 PM
chore: sync dev with main (#1346), 2400 files modified

### Commit 2: [497e2e4](https://github.com/vig-os/devkit/commit/497e2e4241d9254f35ba817a98eb3e6b6572bd03) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 5, 2026 at 05:21 AM
chore: sync issues and PRs, 252 files modified (docs/issues/issue-529.md, docs/pull-requests/pr-1341.md, docs/pull-requests/pr-1346.md)

### Commit 3: [b5f1bc9](https://github.com/vig-os/devkit/commit/b5f1bc9e874246686f3d531ee292d19837b64fcd) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 6, 2026 at 05:23 AM
chore: sync issues and PRs, 81 files modified (docs/issues/issue-1347.md, docs/issues/issue-529.md)

### Commit 4: [ebd3c1e](https://github.com/vig-os/devkit/commit/ebd3c1eb39da86d027df10283537543bc681cc64) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 7, 2026 at 04:30 AM
chore: sync issues and PRs, 10 files modified (docs/issues/issue-529.md)

### Commit 5: [d429f21](https://github.com/vig-os/devkit/commit/d429f2112207f8419dc59ad5837844220fe18edd) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:13 AM
test(upgrade): pin no-diff adoption-issue cleanup, 47 files modified (tests/test_workflow_devkit_upgrade.py)

### Commit 6: [2bb1508](https://github.com/vig-os/devkit/commit/2bb15082a6aa7b7e69fb18911035e770cdedc746) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:15 AM
fix(upgrade): close the adoption issue a no-diff run created, 61 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/devkit-upgrade.yml)

### Commit 7: [c902d54](https://github.com/vig-os/devkit/commit/c902d54df3de3ee994d32cff754bda5694aa7d1d) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:19 AM
test(install): pin retired-scaffold-path pruning on upgrade, 131 files modified (tests/test_scaffold_retired_paths.py, tests/workflow_scaffold.py)

### Commit 8: [294bf42](https://github.com/vig-os/devkit/commit/294bf423fbc75e73f3a033e8327c1a919fb3f683) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:35 AM
fix(install): prune scaffold paths retired since the consumer pin, 186 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md)

### Commit 9: [fb7af79](https://github.com/vig-os/devkit/commit/fb7af7946c116516a014a55d3705f8792bec7e56) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:46 AM
fix(upgrade): close the adoption issue a no-diff run created (#1350), 108 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/devkit-upgrade.yml, tests/test_workflow_devkit_upgrade.py)

### Commit 10: [0cf05dc](https://github.com/vig-os/devkit/commit/0cf05dc776965b2d5f1b31a8e344cc5a4e449ce5) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:50 AM
chore: merge dev into the retired-paths branch, 106 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/devkit-upgrade.yml, tests/test_workflow_devkit_upgrade.py)

### Commit 11: [0c4e6b4](https://github.com/vig-os/devkit/commit/0c4e6b443c106a4a6ec57a61c42ba6fdc7301946) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 09:02 AM
fix(install): prune scaffold paths retired since the consumer pin (#1352), 315 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md, tests/test_scaffold_retired_paths.py, tests/workflow_scaffold.py)

### Commit 12: [2b1b790](https://github.com/vig-os/devkit/commit/2b1b790fba3b34bab0d000774fbd4aac7fd1beb1) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 09:12 AM
test(install): pin content diffs for symlinked preserved-file templates, 45 files modified (tests/bats/init-workspace.bats)

### Commit 13: [8035dfa](https://github.com/vig-os/devkit/commit/8035dfab477486854a1925e5171d637b53f4bee3) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 09:16 AM
fix(install): dereference symlinked templates in preserved-file diffs, 65 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 14: [1269cd3](https://github.com/vig-os/devkit/commit/1269cd3377b2fe654002d80483fb9d452da77663) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 09:32 AM
fix(install): dereference symlinked templates in preserved-file diffs (#1354), 110 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, tests/bats/init-workspace.bats)

### Commit 15: [91e93c5](https://github.com/vig-os/devkit/commit/91e93c54ce1aa1124f1e46ae40f539e629a480ec) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 09:36 AM
test(action): pin dev-shell PATH priority through GITHUB_PATH, 57 files modified (tests/test_setup_toolchain_env.py)

### Commit 16: [9a121e4](https://github.com/vig-os/devkit/commit/9a121e4f6f057a9ae425dab17f246a03a570a367) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 09:38 AM
fix(action): write store bin dirs to GITHUB_PATH in reverse order, 36 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/actions/setup-devkit-toolchain/action.yml)

### Commit 17: [474843f](https://github.com/vig-os/devkit/commit/474843fa73892add52fd0d77eea9e643d0c0d56a) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 09:53 AM
fix(action): write store bin dirs to GITHUB_PATH in reverse order (#1356), 93 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/actions/setup-devkit-toolchain/action.yml, tests/test_setup_toolchain_env.py)

### Commit 18: [6f1d00f](https://github.com/vig-os/devkit/commit/6f1d00f2f72852652b3acff8c8492bb2d6f8bc06) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 10:00 AM
test(action): pin UV_PYTHON out of the shellHook env forward, 45 files modified (tests/test_setup_toolchain_env.py)

### Commit 19: [0f98683](https://github.com/vig-os/devkit/commit/0f98683be125e78b0b800ab3a28f7068d78459fa) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 10:02 AM
fix(action): deny UV_PYTHON in the shellHook env forward, 65 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/actions/setup-devkit-toolchain/action.yml)

### Commit 20: [f8edc6e](https://github.com/vig-os/devkit/commit/f8edc6ea773706cb9b3a45b51c97352ec522b03c) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 10:15 AM
fix(action): deny UV_PYTHON in the shellHook env forward (#1357), 110 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/actions/setup-devkit-toolchain/action.yml, tests/test_setup_toolchain_env.py)

### Commit 21: [1878b47](https://github.com/vig-os/devkit/commit/1878b472fadb3c36ff86c2f6a668579d060dcdea) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 10:19 AM
test(release): pin note extraction against a prefixed changelog heading, 112 files modified (tests/test_release_tag_prefix.py)

### Commit 22: [ded7c0f](https://github.com/vig-os/devkit/commit/ded7c0f72a00331b5408f09c070d09b64bb71eb0) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 10:26 AM
fix(release): extract release notes with the composed tag heading, 48 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/release-publish.yml)

### Commit 23: [4938eec](https://github.com/vig-os/devkit/commit/4938eec3d05693b174b689f978e7f190676f7372) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 10:38 AM
fix(release): extract release notes with the composed tag heading (#1359), 160 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/release-publish.yml, tests/test_release_tag_prefix.py)

### Commit 24: [48fa490](https://github.com/vig-os/devkit/commit/48fa4906934e272cd22c3e12d0674847fb90274d) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 10:57 AM
test(action): pin the remaining Nix-host env leaks out of the forward, 128 files modified (tests/test_setup_toolchain_env.py)

### Commit 25: [552408f](https://github.com/vig-os/devkit/commit/552408f094d0a8a54a1c8e9c5ec79dd2b9d2b416) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 10:59 AM
fix(action): strip store paths and stdenv machinery from the env forward, 108 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/actions/setup-devkit-toolchain/action.yml)

### Commit 26: [bde1a40](https://github.com/vig-os/devkit/commit/bde1a40d0af5c6deea00a037cce0745831a36c24) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 11:12 AM
fix(action): strip store paths and stdenv machinery from the env forward (#1361), 236 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/actions/setup-devkit-toolchain/action.yml, tests/test_setup_toolchain_env.py)

### Commit 27: [7473866](https://github.com/vig-os/devkit/commit/747386670fae5e293b307428fc559f7fd841edb3) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 12:16 PM
test(action): pin NixOS-runner Python toolchain through PATH and uv pins, 134 files modified (tests/test_setup_toolchain_env.py)

### Commit 28: [eb9e5c6](https://github.com/vig-os/devkit/commit/eb9e5c67c4ca9856d847ef096be8b0ecb02d1f1f) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 12:19 PM
fix(action): keep the Nix CPython and uv pins on NixOS runners, 76 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/actions/setup-devkit-toolchain/action.yml)

### Commit 29: [b7b4bbb](https://github.com/vig-os/devkit/commit/b7b4bbb81702b11418ac50e26fb2998fe81f87ac) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 12:32 PM
fix(action): keep the Nix CPython and uv pins on NixOS runners (#1363), 210 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/actions/setup-devkit-toolchain/action.yml, tests/test_setup_toolchain_env.py)

### Commit 30: [bd2278c](https://github.com/vig-os/devkit/commit/bd2278c3d6881e70f54bd7454aeaebee0d159d06) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 12:36 PM
docs(migration): point the env-forward denylist at its source of truth, 59 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md)

### Commit 31: [25b65a9](https://github.com/vig-os/devkit/commit/25b65a9ab3b7f155daa68bb66481795f557a7ed8) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 12:47 PM
docs(migration): point the env-forward denylist at its source of truth (#1364), 59 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md)

### Commit 32: [0ed2a45](https://github.com/vig-os/devkit/commit/0ed2a45c0bc6b52fe826026ecbc99e340c50ec34) by [renovate[bot]](https://github.com/apps/renovate) on August 7, 2026 at 02:20 PM
ci(actions): update github-actions (minor and patch), 40 files modified

### Commit 33: [46a8f3e](https://github.com/vig-os/devkit/commit/46a8f3e7aa69e5bae8d8d6a27348a00b072312bb) by [renovate[bot]](https://github.com/apps/renovate) on August 7, 2026 at 02:20 PM
build(pip): lock file maintenance, 76 files modified (uv.lock)

### Commit 34: [d265c46](https://github.com/vig-os/devkit/commit/d265c461a730535713c2b108a7c850454f5f7008) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 7, 2026 at 02:21 PM
docs(changelog): add unreleased entry for renovate PR 1368, 12 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 35: [46ccf2b](https://github.com/vig-os/devkit/commit/46ccf2b493e8fa3d82534dbf360e062952dc2f46) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:29 PM
build(pip): lock file maintenance (#1369), 76 files modified (uv.lock)

### Commit 36: [732aba3](https://github.com/vig-os/devkit/commit/732aba30d04e86602db13ac8cf6c02474428c267) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:30 PM
ci(actions): update github-actions (minor and patch) (#1368), 52 files modified

### Commit 37: [118e853](https://github.com/vig-os/devkit/commit/118e85336a0240e8b8d7b291ed6b5535762828e7) by [renovate[bot]](https://github.com/apps/renovate) on August 7, 2026 at 02:37 PM
chore(deps): update github/codeql-action digest to 5595cca, 12 files modified (.github/workflows/codeql.yml, .github/workflows/scorecard.yml, assets/workspace/.github/workflows/codeql.yml, assets/workspace/.github/workflows/scorecard.yml)

### Commit 38: [d90ab09](https://github.com/vig-os/devkit/commit/d90ab095825f505e760efdd7ba3570e147f3608d) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 7, 2026 at 02:43 PM
docs(changelog): add unreleased entry for renovate PR 1367, 2 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 39: [6420af3](https://github.com/vig-os/devkit/commit/6420af3f2d6edf58d98621326a02a1370f7b0ec6) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:51 PM
test(ci): pin Test Summary gate against cancelled needed jobs, 81 files modified (tests/test_workflow_summary_gate.py)

### Commit 40: [9b2b0c2](https://github.com/vig-os/devkit/commit/9b2b0c2dfe94b6917b3f3dc690f75051a080fe1b) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:51 PM
fix(ci): fail Test Summary when a needed job is cancelled, 64 files modified (.github/workflows/ci.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 41: [12e4b38](https://github.com/vig-os/devkit/commit/12e4b3826ef0c96666556b609e5ab818e3f3b9c3) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:52 PM
chore(deps): update github/codeql-action digest to 5595cca (#1367), 14 files modified

### Commit 42: [3a8075e](https://github.com/vig-os/devkit/commit/3a8075e593b08e49a96dc32c2a9baf48a574ffd4) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:53 PM
test(release): pin release tag as a lightweight ref, 87 files modified (tests/test_workflow_release_lightweight_tag.py)

### Commit 43: [b03129e](https://github.com/vig-os/devkit/commit/b03129e7ea3be60e5311de0fa14b67bc9ef7de25) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:57 PM
fix(release): publish the release tag as a lightweight ref, 136 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/RELEASE_CYCLE.md)

### Commit 44: [d7ba54f](https://github.com/vig-os/devkit/commit/d7ba54fe4d717ada7f34f9a62c6e98a0cf4f3424) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 03:01 PM
docs: complete the GitHub community profile, 200 files modified

### Commit 45: [d3d9f00](https://github.com/vig-os/devkit/commit/d3d9f00e50730feaf3223f5b0ce75db2f5aa0305) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 03:16 PM
fix(release): publish the release tag as a lightweight ref (#1374), 223 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/RELEASE_CYCLE.md, tests/test_workflow_release_lightweight_tag.py)

### Commit 46: [51f92a6](https://github.com/vig-os/devkit/commit/51f92a6d8e9fe7491f05f6e15bb68494698d24e7) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 03:16 PM
fix(ci): fail Test Summary when a needed job is cancelled (#1375), 145 files modified (.github/workflows/ci.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_workflow_summary_gate.py)

### Commit 47: [0b3cf55](https://github.com/vig-os/devkit/commit/0b3cf554396a178a36785c062fe51a101cfebf76) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 03:17 PM
docs: complete the GitHub community profile (#1376), 200 files modified

### Commit 48: [33ecbe5](https://github.com/vig-os/devkit/commit/33ecbe5bd1f8476acce69e522a81f64e5bb5dbee) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:40 PM
test(workflows): require client-id App credentials in sync and upgrade workflows, 116 files modified (tests/test_workflow_client_id_credentials.py, tests/test_workflow_devkit_upgrade.py)

### Commit 49: [17ad1d5](https://github.com/vig-os/devkit/commit/17ad1d5a07db1c701c39b6cd97ea3bf8f772ac24) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:41 PM
feat(workflows): switch App credentials to client-id with legacy fallback, 24 files modified (.github/workflows/sync-issues.yml, assets/workspace/.github/workflows/devkit-upgrade.yml, assets/workspace/.github/workflows/sync-issues.yml)

### Commit 50: [11aff07](https://github.com/vig-os/devkit/commit/11aff07635d07eb7f44426684f9e1a07d5b3e479) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 02:42 PM
docs(secrets): drop the numeric App-ID requirement from secret references, 38 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/docs/DOWNSTREAM_RELEASE.md, docs/DOWNSTREAM_RELEASE.md, docs/RELEASE_CYCLE.md, docs/security/ADR-secrets-management.md)

### Commit 51: [43dbce3](https://github.com/vig-os/devkit/commit/43dbce3b2e52d81317627aff9b0a93457a96c38c) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 03:57 PM
feat(workflows): switch App credentials to client-id with legacy fallback (#1373), 178 files modified

### Commit 52: [f903ef0](https://github.com/vig-os/devkit/commit/f903ef0cb154a7a4470317593dda3c47e378e2f1) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:06 PM
test(workflows): pin git-push floating-tag moves with explicit App token, 54 files modified (tests/test_floating_tags.py)

### Commit 53: [e76c31d](https://github.com/vig-os/devkit/commit/e76c31da2884d12970f70665de28c097fadf9d41) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:12 PM
fix(workflows): move floating tags via git push with explicit App token, 110 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/promote-release.yml, docs/MIGRATION.md)

### Commit 54: [75db79f](https://github.com/vig-os/devkit/commit/75db79fe8da4867bab4a34da85b24f44cfa6ceb4) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:13 PM
test(workflows): pin scaffold release tag as a lightweight ref, 201 files modified (tests/test_release_tombstone_detection.py, tests/test_workflow_release_publish_lightweight_tag.py)

### Commit 55: [7898df8](https://github.com/vig-os/devkit/commit/7898df8acfbc153200dae441b33bf044dadf33e2) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:24 PM
fix(workflows): publish the scaffold release tag as a lightweight ref, 174 files modified

### Commit 56: [1b027a4](https://github.com/vig-os/devkit/commit/1b027a47155987282b1c66b248af13235ce55d3d) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:28 PM
test(bats): unset IN_NIX_SHELL in githooks guard-blocking cases, 18 files modified (tests/bats/githooks.bats)

### Commit 57: [4ca5ca5](https://github.com/vig-os/devkit/commit/4ca5ca52a2d20af728fd00fe840e01ec3e4a590c) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:28 PM
fix(workflows): move floating tags via git push with explicit App token (#1379), 164 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/promote-release.yml, docs/MIGRATION.md, tests/test_floating_tags.py)

### Commit 58: [c883683](https://github.com/vig-os/devkit/commit/c88368324ae9e1cb661b2bf38bb78cecadbe8616) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:35 PM
fix(workflows): publish the scaffold release tag as a lightweight ref (#1382), 375 files modified

### Commit 59: [8edb727](https://github.com/vig-os/devkit/commit/8edb7272e628713e4f52385aea13a934d7a3e710) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:26 PM
fix(hooks): rename pymarkdown excludes to CONTRIBUTING.md in flake mirror, 22 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.pre-commit-config.yaml, nix/hooks.nix)

### Commit 60: [f7a9379](https://github.com/vig-os/devkit/commit/f7a93795f536e40d53a6f1ee1d4c421231d3ed9b) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:38 PM
test(bats): unset IN_NIX_SHELL in githooks guard-blocking cases (#1384), 18 files modified (tests/bats/githooks.bats)

### Commit 61: [046417c](https://github.com/vig-os/devkit/commit/046417c35434f583874cb886da37fcc5888ce3cb) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 04:46 PM
fix(hooks): rename pymarkdown excludes to CONTRIBUTING.md in flake mirror (#1383), 22 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.pre-commit-config.yaml, nix/hooks.nix)

### Commit 62: [81b90df](https://github.com/vig-os/devkit/commit/81b90df049302495cfc677117d0021762b72ef81) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 7, 2026 at 04:49 PM
chore: freeze changelog for release 1.7.0, 18 files modified (CHANGELOG.md)

### Commit 63: [7b3d2cc](https://github.com/vig-os/devkit/commit/7b3d2ccad9abfd2f8d159e0aeeeb3da1509a687c) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 7, 2026 at 04:50 PM
chore: sync workspace manifest for release 1.7.0, 18 files modified (assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 64: [964c7d4](https://github.com/vig-os/devkit/commit/964c7d4807030dd921f8384820b202e5cac8d61d) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 05:16 PM
fix(security): except CVE-2026-66032 in the vulnix register, 42 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 65: [8d78123](https://github.com/vig-os/devkit/commit/8d78123250428cf774818e3994253b507d280bc0) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 05:27 PM
fix(security): except CVE-2026-66032 in the vulnix register (#1387), 42 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 66: [a9adf8c](https://github.com/vig-os/devkit/commit/a9adf8ca3d82e50ec18b5b26308a839f06c73f4c) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:10 PM
test(bats): require human-approval gate in smoke dispatch listener, 5 files modified (tests/bats/just.bats)

### Commit 67: [041f69d](https://github.com/vig-os/devkit/commit/041f69de9bd0d4b82e894b985bf6349e936e8565) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:12 PM
fix(smoke-test): gate final release on human PR approval, 61 files modified (CHANGELOG.md, assets/smoke-test/.github/workflows/repository-dispatch.yml, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 68: [062b243](https://github.com/vig-os/devkit/commit/062b243a4810099eedfe1ea903aa13814ab2e787) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:23 PM
fix(smoke-test): gate final release on human PR approval (#1392), 66 files modified (CHANGELOG.md, assets/smoke-test/.github/workflows/repository-dispatch.yml, assets/workspace/.devcontainer/CHANGELOG.md, tests/bats/just.bats)

### Commit 69: [88eea95](https://github.com/vig-os/devkit/commit/88eea95496a188c730bddf97f839d5361d78a464) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 7, 2026 at 09:29 PM
chore: finalize release 1.7.0, 8 files modified (.vig-os, CHANGELOG.md, README.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 70: [daeb5c5](https://github.com/vig-os/devkit/commit/daeb5c541f6a4ec2cf31271fca47f8f22235bb2c) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 7, 2026 at 09:31 PM
chore: sync issues and PRs, 4056 files modified
