---
type: pull_request
state: closed (merged)
branch: feature/636-multi-arch-nix-image → feature/625-nix-claude-migration
created: 2026-06-23T16:21:24Z
updated: 2026-06-23T17:27:35Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devcontainer/pull/659
comments: 1
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-06-23T17:27:33Z
synced: 2026-06-24T06:13:17.952Z
---

# [PR 659](https://github.com/vig-os/devcontainer/pull/659) ci(nix): multi-arch Nix image discovery index (amd64 + arm64) (#636)

## Summary

Implements **T2.3 (#636)**: the `Nix Image (discovery)` workflow now produces a **multi-arch index** (amd64 + arm64) of the Nix-built devcontainer image, so downstream digest-pinning can resolve against a top-level OCI index.

- Build `packages.devcontainerImage` on a **native** matrix — `ubuntu-24.04` (amd64) + `ubuntu-24.04-arm` (arm64), no QEMU / cross-compilation, so each per-arch image config carries the correct `architecture`.
- Push per-arch **discovery** tags `nix-dev-amd64` / `nix-dev-arm64`, then assemble the index `nix-dev` with `docker buildx imagetools create` (mirrors the proven `release.yml` flow).
- Verify the index advertises both `linux/amd64` and `linux/arm64` via `imagetools inspect`.
- `cachix-action` runs with an auth token on every leg, so the **arm64 closure lands in the `vig-os` cache**.

### Scope guardrails
- Stays **`continue-on-error`** (discovery phase) and only ever touches the **disposable `nix-dev*` tags**. The versioned / `:latest` **publish-cutover is #639** — explicitly out of scope here.

## Dependencies
- Depends-on: #634 (✅ merged). Blocks: #639.

## Acceptance criteria
- [x] amd64 + arm64 native CI matrix building `packages.devcontainerImage`
- [x] per-arch tags pushed
- [x] multi-arch index assembled with `imagetools create`
- [x] index verified (`imagetools inspect` shows amd64 + arm64)
- [x] arm64 closure pushed to Cachix

## Verification
The multi-arch run executes on push to the epic branch (workflow `paths` include the flake + this workflow) and via `workflow_dispatch`. The `imagetools inspect` step is the AC check (top-level index, not a single-arch manifest).

Refs: #636


---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/devcontainer/pull/659#issuecomment-4781496689) by [@c-vigo](https://github.com/c-vigo)

_Posted on June 23, 2026 at 04:52 PM_

✅ **Verified.** After fixing the per-arch push being skipped on the discovery-phase testinfra failure (`7f8d3cd`), the re-dispatched `Nix Image (discovery)` run is green and the index assembles. Direct inspection of the published index:

\`\`\`
$ docker buildx imagetools inspect ghcr.io/vig-os/devcontainer:nix-dev
Name:      ghcr.io/vig-os/devcontainer:nix-dev
MediaType: application/vnd.docker.distribution.manifest.list.v2+json
  Platform:  linux/amd64
  Platform:  linux/arm64
\`\`\`

Top-level manifest list (not a single-arch manifest) covering both platforms — AC met. The standard `ci.yml` (image) dispatch is also green.

---
---

## Commits

### Commit 1: [6fcd17b](https://github.com/vig-os/devcontainer/commit/6fcd17b738a70a2b5eb82fd280f6c47a952713da) by [c-vigo](https://github.com/c-vigo) on June 23, 2026 at 04:20 PM
ci(nix): build a multi-arch Nix image discovery index (amd64 + arm64), 137 files modified (.github/workflows/nix-image.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 2: [7f8d3cd](https://github.com/vig-os/devcontainer/commit/7f8d3cd26a511a3e1276899b15c5ee46e0333d28) by [c-vigo](https://github.com/c-vigo) on June 23, 2026 at 04:44 PM
ci(nix): keep the per-arch push running after discovery testinfra fails, 3 files modified (.github/workflows/nix-image.yml)
