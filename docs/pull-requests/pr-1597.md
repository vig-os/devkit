---
type: pull_request
state: closed (merged)
branch: release/1.13.0 → main
created: 2026-09-01T14:15:36Z
updated: 2026-09-01T16:15:45Z
author: vig-os-release-app[bot]
author_url: https://github.com/vig-os-release-app[bot]
url: https://github.com/vig-os/devkit/pull/1597
comments: 1
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-09-01T16:15:41Z
synced: 2026-09-02T07:01:40.052Z
---

# [PR 1597](https://github.com/vig-os/devkit/pull/1597) chore: release 1.13.0

# [Release 1.13.0](https://github.com/vig-os/devkit/releases/tag/1.13.0) - 2026-09-01

This PR prepares release 1.13.0 for merge to main.

## [1.13.0](https://github.com/vig-os/devkit/releases/tag/1.13.0) - 2026-09-01

### Added

- **A `vigos.sesh` project can now open with its own window layout**
  ([#1583](https://github.com/vig-os/devkit/issues/1583))
  - `layout.windows` chose *which* windows a session got, but the choice was
    global: every seed in `sessions` opened identically, so a repo with no
    pull-request workflow still started a dashboard window on a permanently
    empty view, and a docs repo still got a git TUI it had no use for. The only
    escape was disabling the module and hand-writing `sesh.toml`, discarding the
    whole layout mechanism to change one window
  - `vigos.sesh.layout.profiles` takes named window sets and a session's new
    `layout` field selects one; `default` remains `layout.windows` and cannot be
    redefined, so there is one home for the default set rather than two that can
    disagree
  - Delivery is sesh's own config rather than a new mechanism: a session with a
    profile emits `startup_command = "sesh-layout <profile>"`, which sesh
    resolves ahead of `[default_session]`, so a session naming no profile stays
    bare and inherits the default. `sesh-layout` takes the profile as an
    argument and stays a single binary — consumers probe for it by name to
    detect a provisioned host
  - A session naming an undefined profile fails at eval, listing the valid
    names, rather than producing a `sesh.toml` whose sessions die at connect
    time far from the definition that caused it
  - Backward compatible: with `profiles` empty and no session setting `layout`,
    the generated `sesh.toml` and layout script are unchanged
- **`vigos.sesh` can now open projects on remote hosts**
  ([#1585](https://github.com/vig-os/devkit/issues/1585))
  - `vigos.sesh.remotes` declares a project × host × path inventory (`host` is
    an `~/.ssh/config` alias — no connection details in the module); it renders
    to `remotes.tsv` beside `sesh.toml` only when populated, so the empty
    default is a bit-for-bit no-op
  - The picker gains a runner stage that appears only when a project has more
    than one location; the last-used runner is remembered and pre-selected, so
    `Enter Enter` reconnects wherever the session was left running
  - New `sesh-remote-connect` attach-or-creates over SSH (`tmux new -A`),
    probing capability in the same round trip: `sesh-layout` on the remote
    PATH gives the full standard layout, bare tmux a persistent blank session,
    and neither a plain login shell after an explicit warning — no per-host
    capability flags to go stale
  - `vigos.sesh.remoteTerminal` opens the ssh client in its own terminal
    window when picking a remote from inside tmux (a remote tmux must not nest
    in the local popup); the null default uses a local tmux window instead, so
    no personal terminal is baked into the module
- **`vigos.ghdash` now follows the project you are in and takes per-project
  section profiles** ([#1586](https://github.com/vig-os/devkit/issues/1586))
  - New `gh-dash-repo [profile]` launches the dashboard scoped to the repo of
    the launch directory (derived from `origin` — nothing is declared per
    project and no file lands in project repos); outside a GitHub repo it
    falls back to the `repoFilters` scope, so it is always valid
  - `vigos.ghdash.profiles` names alternative section sets (a team repo wants
    review queues a solo repo has no use for); filters are written scope-free
    and the module appends the launch-time scope, so a profile cannot silently
    ship unscoped. A `vigos.sesh` layout profile can point its dashboard
    window at `gh-dash-repo <name>`, so selection rides the session entry that
    already identifies the project
  - Backward compatible: `programs.gh-dash.settings` is unchanged, bare
    `gh-dash` keeps the `repoFilters` scope, and a per-repo scope is cheaper
    than the widened filter it replaces
- **A `vigos.ghdash` profile can now filter its issues view separately**
  ([#1595](https://github.com/vig-os/devkit/issues/1595))
  - A profile named one section list and the wrapper wrote it to both views,
    but PR and issue queues are not filtered alike: the qualifiers that make a
    PR profile useful (`review-requested:@me`, `draft:false`) either do not
    apply to issues or mean something else, so a profile written for pull
    requests landed a permanently empty section under Issues — the only escape
    was writing the profile down to what both views understand
  - A profile may now be `{ prSections; issuesSections; }` instead of a bare
    list, with `issuesSections = [ ]` leaving the issues view empty rather
    than wrong; issue filters are scope-free and get the launch-time scope
    exactly as PR ones do
  - The `default` profile keeps a consumer-set
    `programs.gh-dash.settings.issuesSections` instead of overwriting it —
    those sections carry the scope their author chose, and one that wants to
    follow the launch repo writes the `__GH_DASH_SCOPE__` placeholder itself.
    Since the `prs` window of a `vigos.sesh` layout runs `gh-dash-repo`, that
    was the launch path where the setting silently disappeared
  - Backward compatible: a bare list still means both views, so every profile
    written against #1586 renders byte-identically
- **CI now guards `homeManagerModules` on home-manager master + nixos-unstable**
  ([#1589](https://github.com/vig-os/devkit/issues/1589))
  - The `vigos.*` home modules are exported as paths and evaluated against
    whatever nixpkgs and home-manager the consumer supplies, but devkit only
    tested the stable side of that contract — an option rename on home-manager
    `master`, or an unstable nixpkgs change under a module default, surfaced as
    a consumer eval break after a lock bump, downstream
  - A new eval-only `nixosConfigurations.ci-hm-unstable` wires the full module
    set (every `enable` on) through the NixOS-module tier —
    `home-manager.users.<name>`, the wiring the production consumer runs — on
    home-manager `master` + `nixpkgs-unstable`, and a blocking Tier-0 test
    forces its toplevel drvPath; nothing is built
  - The weekly `update-nixpkgs-unstable` cron now bumps the new
    `home-manager-unstable` input alongside `nixpkgs-unstable`, so a
    master-side break surfaces on that PR's CI, inside devkit's release gate,
    before any consumer lock bump

### Changed

#### Dependencies

- Update `@devcontainers/cli` from `0.88.0` to `0.89.0` ([#1582](https://github.com/vig-os/devkit/pull/1582))

### Security

- **Except the rsync 3.5.0 advisory batch in the vulnix register**
  ([#1592](https://github.com/vig-os/devkit/issues/1592),
  [#1593](https://github.com/vig-os/devkit/issues/1593))
  - The NVD feed published 17 new CVEs against `rsync-3.4.4` overnight, taking
    both nightly scan lanes and the release train's `vulnix-gate` red; 8 score
    >= 7.0 and are now excepted, the other 9 fall below the gate's threshold
  - A feed event, not a closure change: the previous night's scan was green on
    the same pin and the findings diff is 17 added / 0 removed / no other
    package touched, so no exception had expired and nothing in this repo
    caused it
  - Advancing the pin cannot clear them today — all 17 are fixed in rsync
    3.5.0, whose 26.05 backport reached `staging-next-26.05` on 2026-08-29 but
    not yet `release-26.05` or the pinned `nixos-26.05`
  - Expires `2026-09-23`, its own early slot on the staggered grid because this
    block's lever moves soonest: it should die on the pin advance that ships
    3.5.0 rather than be renewed. Six of the eight defects are `rsyncd`
    daemon-only and this image never starts a daemon; the remaining two need
    the user to point rsync at a hostile peer



---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/devkit/pull/1597#issuecomment-5496060147) by [@c-vigo](https://github.com/c-vigo)

_Posted on September 1, 2026 at 03:08 PM_

### RC1 validation — home-module tier

`1.13.0-rc1` (`7da731ef`) is green on every automated lane: all release jobs
including **Vulnix CVE Gate**, and the cross-repo gate (smoke-test ran Prepare
Release → CI → Direnv Smoke → Release, all success, `1.13.0-rc1` pre-release
created).

Those lanes do not cover this train's headline content. `vigos.sesh` and
`vigos.ghdash` ship as `homeManagerModules`; the devcontainer image is built
from `devTools` via `dockerTools.buildLayeredImage`, so neither the RC image nor
the smoke test touches #1583/#1585/#1586/#1595. From the image's side this is a
dependencies-only train. The checks below cover the rest.

Method: `checks.x86_64-linux.hm-full` (= `homeConfigurations.ci-full-x86_64-linux.activationPackage`)
built from the release branch, plus a fixture layered on it with `extendModules`
— `ci-full` only flips `enable` flags, so it never reaches the new option
surface. Binaries were run from their store paths; no home profile was activated.

#### Backward compatibility

Built `hm-full` from `1.12.0` and from `release/1.13.0` and diffed the rendered
files:

| Artifact | Result |
|---|---|
| `.config/sesh/sesh.toml` | **bit-for-bit identical** |
| `.config/gh-dash/config.yml` | **identical** |
| `.config/sesh/remotes.tsv` | absent when `remotes` is empty |
| `.config/gh-dash/profiles/` | new directory, purely additive |

The "empty inventory is a bit-for-bit no-op" claim is now tested rather than
asserted.

#### Rendering (#1585, #1586, #1595)

- `remotes.tsv` renders tab-separated `project<TAB>host<TAB>path`, one line per
  project × host.
- Bare-list profile mirrors its sections to both views; the
  `{ prSections; issuesSections; }` form keeps them apart; `issuesSections = [ ]`
  renders a genuinely empty issues view.
- Every profile filter is emitted scope-free carrying `__GH_DASH_SCOPE__`.
- A consumer-set `programs.gh-dash.settings.issuesSections` passes through the
  `default` template **verbatim**, with no placeholder appended, while
  `prSections` keeps the generated set — the deliberate asymmetry in #1595.

#### Runtime

- `gh-dash-repo <profile>` inside this repo substitutes `repo:vig-os/devkit`
  into both views, per-view filters preserved.
- Outside a GitHub repo it falls back to the rendered `repoFilters`
  (`involves:@me` with the option at its default), empty issues view preserved.
- Unknown profile exits **1** and lists the valid profiles.
- `sesh-layout` builds the default 4 windows (edit/git/shell/claude);
  `sesh-layout docs` builds the 2-window profile (edit/shell).
- A session naming a profile emits `startup_command = "sesh-layout docs"`;
  a session naming none stays bare.
- A session selecting an undefined layout profile fails at **eval**:
  `session "oops" selects layout profile "typo", which is not defined. Valid profiles: default, docs.`
- Picker with `remoteTerminal = null` dispatches
  `tmux new-window -n "$name" sesh-remote-connect …` inside tmux, direct exec
  outside — no nested remote tmux.

#### `sesh-remote-connect` tiered probe

Exercised against a provisioned host on the internal network:

1. **`sesh-layout` on the remote PATH** — verified end to end: a live 4-window
   standard layout session was created on the remote and then torn down.
2. **tmux only** — dispatches `new-session -A -s <name> -c <path>`, correctly
   without the `sesh-layout` argument.
3. **neither** — emits the warning and drops to a login shell.

Tiers 2 and 3 were forced by running the dispatch snippet verbatim from the
installed script with the probe targets masked.

Incidental: the remote tmux would not start until `TERM` was set, which is
exactly what the script's `export TERM=xterm-256color` exists to guarantee.

#### One observation — follow-up, not a blocker

Redefining the reserved `default` profile is **silently ignored**: setting
`vigos.ghdash.profiles.default` produced a byte-identical build to the untouched
one. Both modules use `cfg.profiles // { default = …; }`, so the generated set
always wins, and both option descriptions document it. The asymmetry is that
selecting an *undefined* sesh layout profile throws a clear eval error, while
redefining a *reserved* name says nothing at all. Safe direction; worth a
follow-up issue for a later train.


---
---

## Commits

### Commit 1: [a73e5b5](https://github.com/vig-os/devkit/commit/a73e5b555f0e32d3e9846e9f93f7d88ae11f1d20) by [renovate[bot]](https://github.com/apps/renovate) on August 31, 2026 at 08:07 AM
build(npm): update dependency @devcontainers/cli to v0.89.0, 10 files modified (package-lock.json, package.json)

### Commit 2: [7d50c75](https://github.com/vig-os/devkit/commit/7d50c75d70cc5b6141762252afe0c237580bbc97) by [vig-os-release-app[bot]](https://github.com/apps/vig-os-release-app) on August 31, 2026 at 08:12 AM
chore: sync dev with main (#1581), 537 files modified

### Commit 3: [868ba46](https://github.com/vig-os/devkit/commit/868ba466ea3e6118e63bd2952cad1ae23f593538) by [c-vigo](https://github.com/c-vigo) on August 31, 2026 at 08:20 AM
build(npm): update dependency @devcontainers/cli to v0.89.0 (#1582), 10 files modified (package-lock.json, package.json)

### Commit 4: [6275aa5](https://github.com/vig-os/devkit/commit/6275aa599a52ddac9984d0870a9915db482fffcb) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 31, 2026 at 08:32 AM
chore: sync issues and PRs, 208 files modified (docs/issues/issue-529.md, docs/pull-requests/pr-1580.md, docs/pull-requests/pr-1581.md, docs/pull-requests/pr-1582.md)

### Commit 5: [349f058](https://github.com/vig-os/devkit/commit/349f0581ee2532494ae875e44f68840df8efc2cb) by [c-vigo](https://github.com/c-vigo) on August 31, 2026 at 08:57 AM
test(sesh): cover per-session layout profiles, 91 files modified (tests/nix_helpers.py, tests/test_flake_checks.py)

### Commit 6: [5e4714a](https://github.com/vig-os/devkit/commit/5e4714adda8eb20a2c388e28caf04eb6548e6ea9) by [c-vigo](https://github.com/c-vigo) on August 31, 2026 at 09:01 AM
feat(sesh): let a session pick its own layout profile, 170 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, nix/home/sesh.nix)

### Commit 7: [9885184](https://github.com/vig-os/devkit/commit/988518434950a52f7d18ada91e07b98e15c6ac62) by [c-vigo](https://github.com/c-vigo) on August 31, 2026 at 09:18 AM
feat(sesh): let a session pick its own layout profile (#1584), 261 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, nix/home/sesh.nix, tests/nix_helpers.py, tests/test_flake_checks.py)

### Commit 8: [960814b](https://github.com/vig-os/devkit/commit/960814b03e322a50755caaa3d179be34eb2be560) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 31, 2026 at 11:01 AM
build(nix): advance pinned nixpkgs rev (weekly refresh), 6 files modified (flake.lock)

### Commit 9: [a606d49](https://github.com/vig-os/devkit/commit/a606d49ce95955a17e39389a5b83fb1067dfd71a) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on August 31, 2026 at 11:18 AM
chore(nix): bump nixpkgs-unstable (fast-movers refresh), 6 files modified (flake.lock)

### Commit 10: [478b13a](https://github.com/vig-os/devkit/commit/478b13ae3d4abab6b82fb003bf105fc32245baac) by [c-vigo](https://github.com/c-vigo) on August 31, 2026 at 08:23 PM
build(nix): advance pinned nixpkgs rev (weekly refresh) (#1587), 6 files modified (flake.lock)

### Commit 11: [5293f35](https://github.com/vig-os/devkit/commit/5293f356b76ad15dcf6db823f951c5ddd86b91f6) by [c-vigo](https://github.com/c-vigo) on August 31, 2026 at 08:23 PM
chore(nix): bump nixpkgs-unstable (fast-movers refresh) (#1588), 6 files modified (flake.lock)

### Commit 12: [f5d736e](https://github.com/vig-os/devkit/commit/f5d736eab65c3d0d5df9997a02abfb905eec4bec) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on September 1, 2026 at 07:38 AM
chore: sync issues and PRs, 480 files modified (docs/issues/issue-1583.md, docs/issues/issue-1585.md, docs/issues/issue-1586.md, docs/issues/issue-529.md, docs/pull-requests/pr-1584.md, docs/pull-requests/pr-1587.md, docs/pull-requests/pr-1588.md)

### Commit 13: [3c68b60](https://github.com/vig-os/devkit/commit/3c68b606fcf9b77a317a586c88b4b5343babc151) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:28 AM
test(ci): assert homeManagerModules evaluate on HM master + unstable, 45 files modified (tests/test_flake_checks.py)

### Commit 14: [4d93ed5](https://github.com/vig-os/devkit/commit/4d93ed543e79a623ee42d46dfb57fc6b2413478b) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:30 AM
feat(ci): add HM master + nixos-unstable eval guard for home modules, 80 files modified (flake.lock, flake.nix)

### Commit 15: [7e8224d](https://github.com/vig-os/devkit/commit/7e8224db420f22258b97bbd3bdd713def495afa4) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:31 AM
ci: bump home-manager-unstable in the weekly fast-branch refresh, 56 files modified (.github/workflows/update-nixpkgs-unstable.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 16: [cdb3ca5](https://github.com/vig-os/devkit/commit/cdb3ca548ddfb139491027fb6728f6defa9d6938) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:34 AM
style(nix): merge ci-hm-unstable dotted keys for the statix gate, 33 files modified (flake.nix)

### Commit 17: [21e2d7d](https://github.com/vig-os/devkit/commit/21e2d7d1830dac799f0d9ddd8443ec88277c255a) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:41 AM
test(sesh): cover remote seeds inventory and dispatch script, 84 files modified (tests/test_flake_checks.py)

### Commit 18: [982af67](https://github.com/vig-os/devkit/commit/982af679dbe61a8e074c826a864c51dab1f4b189) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:44 AM
feat(sesh): remote project seeds and a runner-aware picker, 264 files modified (nix/home/sesh.nix)

### Commit 19: [4cd5b3b](https://github.com/vig-os/devkit/commit/4cd5b3ba7f149465992d2a8e4511c8afed620071) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:45 AM
test(ghdash): cover profile templates and scope-substituting wrapper, 55 files modified (tests/test_flake_checks.py)

### Commit 20: [201c685](https://github.com/vig-os/devkit/commit/201c685c9ae6c04704f29f0951d88e67f5b9ffdf) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:46 AM
feat(ghdash): per-project scope wrapper and section profiles, 166 files modified (nix/home/ghdash.nix)

### Commit 21: [ed12d52](https://github.com/vig-os/devkit/commit/ed12d5263527b2a8017e8f8f85dd0ca8f6f81d22) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:47 AM
docs(changelog): record sesh remotes and ghdash profiles, 66 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 22: [7b50ce8](https://github.com/vig-os/devkit/commit/7b50ce8c23f75f8296f19a312e94044f91c5d4e6) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:48 AM
feat(ci): evaluate homeManagerModules against home-manager master + nixos-unstable (#1590), 184 files modified (.github/workflows/update-nixpkgs-unstable.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.lock, flake.nix, tests/test_flake_checks.py)

### Commit 23: [2b3cfcf](https://github.com/vig-os/devkit/commit/2b3cfcf7b4a5cd21c520ca30d89b2af6ac82ee4e) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 08:50 AM
chore: merge dev (devkit 1590 eval guard) into the topic branch, 183 files modified (.github/workflows/update-nixpkgs-unstable.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.lock, flake.nix, tests/test_flake_checks.py)

### Commit 24: [5c8e269](https://github.com/vig-os/devkit/commit/5c8e269e48ced59da84cf5d289fc7d1c099a0136) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 09:06 AM
feat(sesh): remote project seeds and per-project gh-dash scope/profiles (#1591), 634 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, nix/home/ghdash.nix, nix/home/sesh.nix, tests/test_flake_checks.py)

### Commit 25: [7565e7f](https://github.com/vig-os/devkit/commit/7565e7f0d32501acbb05c49bcdea368c7393c4c3) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 12:16 PM
fix(security): except the rsync 3.5.0 advisory batch in the vulnix register, 118 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 26: [fc8c9e7](https://github.com/vig-os/devkit/commit/fc8c9e7fa427b2a6fae62e8a7b9c3dad7536351a) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 12:43 PM
fix(security): except the rsync 3.5.0 advisory batch in the vulnix register (#1594), 118 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 27: [7a9aaa1](https://github.com/vig-os/devkit/commit/7a9aaa16828b6aa6f960b3fc1f0af9f5436e15d4) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 12:57 PM
test(ghdash): cover split profile sections and consumer issuesSections, 79 files modified (tests/test_flake_checks.py)

### Commit 28: [12a64b9](https://github.com/vig-os/devkit/commit/12a64b9a128add07c65ecbcd7c2c8c77bd1ff14c) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 01:01 PM
feat(ghdash): let a profile carry its own issuesSections, 120 files modified (nix/home/ghdash.nix)

### Commit 29: [cb6b520](https://github.com/vig-os/devkit/commit/cb6b520a496a7ee9374b7c20f7df316610ce5604) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 01:01 PM
docs(changelog): record per-view gh-dash profile sections, 40 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 30: [2b7ae1f](https://github.com/vig-os/devkit/commit/2b7ae1fc5c9143f9eedaa50c1915cd476a6a0780) by [c-vigo](https://github.com/c-vigo) on September 1, 2026 at 01:59 PM
feat(ghdash): per-view section lists for gh-dash profiles (#1596), 239 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, nix/home/ghdash.nix, tests/test_flake_checks.py)

### Commit 31: [592c765](https://github.com/vig-os/devkit/commit/592c765af2685dd2475538ee919935d5021aac4d) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on September 1, 2026 at 02:14 PM
chore: freeze changelog for release 1.13.0, 20 files modified (CHANGELOG.md)

### Commit 32: [7da731e](https://github.com/vig-os/devkit/commit/7da731efc0def3364e95e0cca97134dd4cb63aae) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on September 1, 2026 at 02:15 PM
chore: sync workspace manifest for release 1.13.0, 20 files modified (assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 33: [27b9fb4](https://github.com/vig-os/devkit/commit/27b9fb4cb44879b5b77e0681ab38095ba323ee60) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on September 1, 2026 at 03:11 PM
chore: finalize release 1.13.0, 8 files modified (.vig-os, CHANGELOG.md, README.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 34: [8a2c49b](https://github.com/vig-os/devkit/commit/8a2c49beba31b7a0d9ffda54400a6a718846e54e) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on September 1, 2026 at 03:13 PM
chore: sync issues and PRs, 1081 files modified
